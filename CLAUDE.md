# pipeline-diff 프로젝트 규칙

## 프로젝트 개요
공공영업1팀 주간 파이프라인 변동이력 분석 웹사이트.
전주/금주 엑셀 파일(인벤토리 또는 매출목록)을 업로드하면
신규·삭제·금액증가·금액감소 항목을 자동으로 분석해 표시한다.

## GitHub 정보
- 계정: soyea0529-it
- 레포지토리: soyea0529-it/pipeline-diff
- 배포: GitHub Pages (main 브랜치 기준)
- 배포 URL: https://soyea0529-it.github.io/pipeline-diff

## 파일 구조
- `pipeline-diff.html` — 메인 웹사이트 파일 (단일 파일로 모든 기능 포함)

## 기술 스택
- 순수 HTML/CSS/JavaScript (서버 불필요)
- xlsx.js 라이브러리 (엑셀 파일 읽기)
- GitHub Pages로 무료 배포

## 데이터 구조

### 인벤토리 파일 컬럼
- 키: 프로젝트코드
- 금액: 총 합계 (백만원 단위)
- 주요 컬럼: 고객사명, 프로젝트명, 영업담당자, 수주확도

### 매출목록 파일 컬럼
- 키: 매출 번호
- 금액: 합계 (원 단위)
- 주요 컬럼: 고객사명, 프로젝트명, 매출실적영업대표, 상태

## 변동 분류 기준
- 신규: 전주에 없고 금주에 있는 항목
- 삭제: 전주에 있고 금주에 없는 항목
- 증가: 금액이 증가한 항목
- 감소: 금액이 감소한 항목
- 유지: 변동 없는 항목

## 코드 작성 규칙
- 단일 HTML 파일로 유지 (pipeline-diff.html)
- 외부 라이브러리는 CDN만 사용 (설치 불필요)
- 한국어 UI 유지
- 다크 테마 유지 (배경색 #0f1117 기준)
- 금액 표시: 인벤토리는 백만원, 매출은 억/백만원 단위로 자동 변환

## GitHub 작업 규칙
- 작업 완료 후 반드시 main 브랜치에 push
- 커밋 메시지는 한국어로 작성
- 예: "인벤토리 탭 영업담당자 컬럼 추가"

## Claude Code 시작 방법 (매번)
1. Windows키 + R → cmd → 엔터
2. `cd pipeline-diff` 입력 → 엔터
3. `claude` 입력 → 엔터

## 작업 히스토리

### 2026-06-09 — 프로젝트 최초 구축
**목표:** 주간 파이프라인 변동이력을 자동으로 분석하는 웹사이트 개발 및 GitHub 배포 환경 구축

**진행 내용:**

1. **웹사이트 설계 및 개발 (claude.ai)**
   - 인벤토리 파일(100%/75%/50%/90%) 및 매출목록 파일 구조 분석
   - `pipeline-diff.html` 단일 파일로 웹사이트 완성
   - 기능: 전주/금주 엑셀 업로드 → 신규·삭제·증가·감소·유지 자동 분류
   - 상단 요약 카드 (건수 + 금액), 필터/검색, CSV 내보내기 포함
   - 다크 테마, 인벤토리/매출 탭 분리 구성

2. **개발 환경 구축 (Windows)**
   - Node.js v24.16.0 설치
   - Claude Code v2.1.169 설치 및 claude.ai 계정 로그인
   - GitHub CLI v2.93.0 설치
   - Git v2.54.0 설치
   - PowerShell 보안 정책 해제 (`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`)

3. **GitHub 연동**
   - GitHub 계정 생성: soyea0529-it
   - 레포지토리 생성: pipeline-diff (Public)
   - `gh auth login` 으로 GitHub CLI 인증 완료
   - `gh repo clone soyea0529-it/pipeline-diff` 로 로컬 클론
   - `/install-github-app` 으로 Claude Code ↔ GitHub Actions 연동 완료
   - GitHub Actions setup complete 확인

**현재 상태:**
- Claude Code에서 명령 → GitHub PR 자동 생성 → 승인(Merge) → 자동 배포 환경 구성 완료
- `pipeline-diff.html` 파일은 아직 GitHub에 올리지 않은 상태 (다음 작업 예정)

**다음 작업 예정:**
- `pipeline-diff.html` GitHub main 브랜치에 업로드
- GitHub Pages 배포 설정 (Settings → Pages → main 브랜치)
- 웹사이트 접속 URL 확인: https://soyea0529-it.github.io/pipeline-diff

---

---

### 2026-06-09 — 2차 작업 (완료)

#### 추가된 기능

**파이프라인 추이 탭**
- 100%/90%/75%/50%(F) 구간별 주차별 잔액 선형 그래프
- 마우스 호버 시 신규/수주/실주 건수 및 금액 표시
- JSON 붙여넣기로 데이터 입력, 로컬스토리지(pipeline_weekly_data) 저장
- 입력 형식: [{"week":"260106","p100":5000,"p90":8000,"p75":12000,"p50":3000,"new":{"count":2,"amount":1500},"won":{"count":1,"amount":800},"lost":{"count":0,"amount":0}}]

**매출 추이 탭**
- 분기 목표선(빨강): Q1 5,253 / Q2 14,178 / Q3 30,101 / Q4 82,068백만 (하드코딩)
- 누적 실적선(초록 점선): 주차별 포인트
- 슬라이더 2개로 가로축 구간 확대/축소
- 주차 포인트 클릭 시 사업기회/매출 현황 상세 패널 표시
- 태그 종류: 수주/실주/신규/확도변경/연도이동/소액변동
- JSON 붙여넣기로 데이터 입력, 로컬스토리지(revenue_weekly_data) 저장

#### 다음 작업 예정
- 인벤토리 비교 프로그램: 전주/금주 엑셀 8개 업로드 → 변동 비교 엑셀 다운로드 + 텍스트 요약
  - 신규/삭제/금액변동/구간이동 자동 분류
  - 제품 감소 항목 강조 표시
  - 구간 이동 추적 (예: 90%→100%)
  - 전주대비 항목별 금액 변동 한줄 요약
- 보고문 자동 초안 생성 기능

#### 인벤토리 파일 구조 (중요)
- 헤더가 3개 행에 걸쳐있음 (row[2]: 기본헤더, row[3]: 금액대분류, row[4]: 금액세부)
- 컬럼 인덱스가 파일(100%/90%/75%/50%)마다 다름 → 반드시 동적으로 탐지해야 함
- 제품합계: row[4]에서 '제품 합계' 찾기
- 상품/용역/유지보수/총합계: row[3]에서 찾기
- 데이터 시작: row[5]부터

---

### 2026-08-03 — 인벤토리 비교 기능 완성

#### 목표
전주/금주 인벤토리(수주확도 7구간: 100%/90%/75%/50%(F)/50%(U)/25%/0%)를 업로드해
신규·완전삭제·금액변동·구간이동을 자동 분류하고, 엑셀 다운로드·보고문 초안까지 생성.

#### 구현 내용
- **업로드**: 전주/금주 각 7슬롯(총 14개), 좌우 2열 배치. 구간별 선택 업로드 가능(미선택 구간은 건너뜀).
- **엑셀 파싱 재작성**: 프로젝트코드 컬럼을 고정 인덱스가 아니라 'OP-'로 시작하는 셀 위치로 동적 탐지하도록 변경(파일마다 컬럼 위치가 달라 하드코딩 불가). 수주상태 필드 추가, 유지관리합계 → 유지보수합계로 명칭 통일.
- **비교 로직**: 전주 7구간 + 금주 7구간을 프로젝트코드 기준으로 통합 비교. 같은 구간이면 유지/변동, 구간이 다르면 삭제가 아니라 "구간이동"으로 분류(유입/유출 양쪽에 기록). 임계값 0.05백만 미만 차이는 변동으로 인정 안 함.
- **화면 표시**: 구간별 탭에 ①전주 대비 금액 변동 한 줄 요약 ②구간 이동 내역(▲유입/★완전신규/▼유출/✗완전삭제) ③데이터 표(구분·고객사명·프로젝트코드·프로젝트명·영업담당자·수주확도·수주상태·비고·제품/상품/용역/유지보수/총합계) ④금주 합계 행.
- **행 색상**: 신규(초록)/삭제(주황+취소선)/변동·구간이동(노랑, 변경된 숫자만 빨간 볼드)/제품 금액 감소(빨강, 최우선순위)/유지(무색).
- **엑셀 다운로드**: 읽기는 xlsx.js 유지, 쓰기는 ExcelJS(CDN 추가)로 전환 — 셀 배경색·빨간볼드·취소선을 화면과 동일하게 반영. 구간별 시트 분리, 구간이동 내역 텍스트 블록 포함. 파일명: `인벤토리_변동비교_전주날짜_금주날짜.xlsx` (파일명에서 6자리 날짜 자동 추출, 실패 시 "전주"/"금주"로 대체).
- **보고문 초안**: 분석 결과 하단에 구간별 텍스트 자동 생성(▲/▼ + 구간이동/신규/변동/유출 내역) + 클립보드 복사 버튼. 유출만 있고 자체 변동 항목이 없는 구간도 순변동액이 올바르게 표시되도록 처리.
- **변동보고서 탭**: 기존 3구간(90/75/50) 전용 로직을 새 데이터 구조에 맞춰 전체 7구간 카드형 리포트로 확장(신규/삭제/구간이동/금액변동 그룹).

#### 검증
- 로컬 정적 서버로 실제 엑셀 픽스처(구간이동, 완전삭제, 제품감소, 유출-only 케이스 포함)를 만들어 브라우저에서 파싱→비교→렌더링→엑셀 다운로드까지 전 과정을 직접 실행해 확인.
- 버그 2건 발견 후 수정: ①구간에서 완전히 빠져나가기만 한 항목(유출)이 있을 때 보고문 초안이 "변동 없음"으로 잘못 표시되던 문제 → 유출 항목도 순변동 사유에 포함하도록 수정. ②구간이동만 있고 실제 금액은 안 바뀐 행의 총합계 셀이 불필요하게 빨간 볼드로 표시되던 문제 → 실제 금액차 있을 때만 볼드 처리하도록 수정.

#### 추가: 업로드/분석 결과 새로고침 유지 (같은 날 후속 작업)
- 인벤토리 비교 탭은 새로고침하면 업로드한 14개 파일이 전부 날아가는 문제가 있어, 파싱된 원본 데이터를 localStorage(`inv_compare_files`)에 자동 저장하도록 변경.
- 페이지 로드 시 저장된 데이터가 있으면 업로드 슬롯 UI를 복원하고, 전주·금주 데이터가 모두 있으면 분석까지 자동으로 다시 실행해서 화면에 그대로 보여줌.
- "초기화" 버튼을 누르면 localStorage도 함께 삭제됨.
- 파이프라인 추이 탭에 이 분석 데이터를 자동 반영하는 기능은 아직 요청 보류 상태(추후 필요 시 진행).

#### 추가: 보고문 초안 표기 개선, 엑셀 셀 색상 수정 (같은 날 후속 작업)
- 보고문 초안에서 같은 구간 내 단순 금액변동 항목은 "전주 X백만 → 금주 Y백만"처럼 나열하던 것을 "▲/▼차액백만" 형태로 축약.
- 엑셀 다운로드 셀 배경색이 웹사이트 다크테마 색을 그대로 써서 흰 배경 엑셀에서 글씨가 안 보이던 문제 수정 → 옅은 파스텔톤(신규=연두, 삭제=연주황, 변동·구간이동=연노랑, 제품감소=연빨강)으로 교체.

---

### 2026-08-03 — 인벤토리 비교 일괄 업로드 + 비교 기간 자동 인식 (3차 작업)

#### 목표
구간별로 파일을 하나씩 올려야 했던 불편을 줄이기 위해 "한번에 7개 업로드" 기능 추가.
동시에 비교 간격이 항상 1주(7일)라고 가정했던 부분을 보완해 임의 간격 비교도 정상 동작하도록 함.

#### 구현 내용
- **일괄 업로드 UI**: 전주/금주 컬럼 상단에 드래그&드롭(다중 파일) 영역 추가. 기존 구간별 개별 슬롯은 그대로 유지 — 일괄 업로드 후에도 개별 슬롯에서 파일을 다시 바꿀 수 있음.
- **파일명 → 구간 자동 배정**: 파일명 마지막 언더바(`_`) 뒤 조각만 보고 구간을 판별(`detectSectionFromFilename`). 100 → 50%F → 50%U → 90 → 75 → 25 → 0 순서로 정확 매칭(대소문자 무시, `%`/`()` 유무 모두 허용). 앞의 날짜(`0714` 등)가 오인식되지 않도록 마지막 조각만 검사.
- **배정 결과 표시**: xlsx가 아닌 파일은 무시 + 회색 안내, 구간 인식 실패 파일은 주황 경고 + 드롭다운으로 수동 배정, 같은 구간에 파일이 2개 이상 배정되면 빨간 경고(첫 파일만 반영, 나중 파일은 무시), 7구간 모두 정상 배정되면 초록색 완료 메시지.
- **날짜 자동 인식**: 파일명 첫 조각이 MMDD 4자리 숫자면 날짜로 인식(`parseFileDateMMDD`). 같은 주(전주/금주) 안에서 파일들의 날짜가 서로 다르면 주황 경고 표시.
- **비교 기간 표시**: 화면 상단에 "비교 기간: 7월 7일 → 7월 14일 (1주 간격)" 형태로 표시. 7일이 아니면 간격 부분을 주황색으로 강조. 14일이면 "2주 간격", 그 외에는 "N일 간격".
- **"전주 대비" 문구 동적화**: 7일 간격이면 기존처럼 "전주 대비"/"전주", 14일이면 "2주 전 대비"/"2주 전", 그 외에는 "이전 회차 대비"/"이전 회차". 구간별 요약 헤더, 보고문 초안, 표 비고 열의 "전주 X → 금주 Y" 표현까지 전부 이 규칙을 따르도록 통일(`invPrevLabel`/`invCompareLabel` 함수로 일원화).
- **엑셀 파일명**: 인식된 실제 날짜를 반영(`인벤토리_변동비교_0630_0714.xlsx`). 날짜 인식 실패 시 오늘 날짜(MMDD)로 대체.

#### 검증
- 로컬 정적 서버 + 합성 File 객체로 일괄 업로드 시나리오(정상 7개, 중복 배정, 인식 실패+수동 배정, non-xlsx 혼입)를 직접 실행해 배정 결과·경고 문구·슬롯 반영을 확인.
- 7일/14일/10일 간격 각각에 대해 비교 기간 문구·경고 스타일·"전주 대비" 라벨이 올바르게 바뀌는지 확인.
- 같은 주 안에서 날짜가 다른 파일을 섞어 업로드했을 때 불일치 경고가 뜨는지, 엑셀 파일명에 인식된 날짜가 정확히 들어가는지, 날짜 인식 실패 시 오늘 날짜로 대체되는지 확인.
- 새로고침 후에도 일괄 업로드로 채운 데이터와 비교 기간 표시가 그대로 복원되는지 확인.

---

### 2026-08-03 — 매출 추이 탭 개편 + 파이프라인 추이 탭 개편중 처리 (4차 작업)

#### 매출 추이 탭: 목표 대비 실적 차트로 전면 개편
기존 JSON 붙여넣기 + 슬라이더 방식을 완전히 걷어내고, 주간 영업회의자료 엑셀 1개 업로드 →
목표 대비 매출 추이 자동 분석으로 교체 (기존 revenue-trend 관련 HTML/JS는 삭제, 복구 대상 아님).

- **업로드**: 파일 1개, 드래그&드롭 + 클릭 선택.
- **시트 자동 탐지**: 시트명에 "실적" 또는 "매출예상" 포함 시트 사용, 없으면 두 번째 시트(index 1).
- **목표 상수**: `SALES_TARGET_2026` (본부/팀1 × 제품/상품/용역/유지보수 × 분기, 백만원 단위). 본부 연간 합계 82,068백만 / 팀1 연간 합계 29,062백만 — 코드 작성 후 직접 합산해 검증함.
- **본부 실적 파싱**: 상단 20행 이내에서 "월" 셀 오른쪽에 제품·상품·용역·유지관리·합계가 순서대로 있는 행을 헤더로 동적 탐지, 그 아래 "N월" 형태 행만 추출(분기/반기 소계 행은 패턴이 달라 자동 제외).
- **1팀 실적 파싱**: A열이 "NO"인 헤더행에서 프로젝트코드/매출청구일/제품/상품/용역/유지관리/합계/매출실적영업대표 컬럼 위치를 동적 탐지. 프로젝트코드가 'OP-'로 시작하는 행만 유효 데이터로 인정, 매출실적영업대표가 1팀 명단(조영오·양소예·정인수, 공백 제거 후 비교)에 있는 행만 매출청구일 기준 월별 합산.
- **당월 예상액**: A열에 "매출 예상액"이 포함된 행(중복 시 첫 번째만 사용)에서 해당 월을 파싱. 본부 기준은 그 행 값을 그대로 사용하지만, 1팀 기준일 때는 그 행이 본부 전체 값이라 그대로 쓰면 틀리므로 1팀 상세행을 해당 월 + 1팀 3명 조건으로 재집계.
- **목표의 월별 배분**: 목표는 분기 단위뿐이므로, 월별 목표선은 분기 목표를 3으로 균등 배분한 "참고값"으로만 표시(빨간 점선). 달성률은 항상 분기 누적 기준으로만 계산.
- **차트 3종(Chart.js)**: ①월별 실적(파랑 막대) vs 예상월(회색+점선 테두리 막대) vs 목표참고선(빨간 점선) — 막대 위 금액 라벨은 커스텀 Chart.js 플러그인으로 직접 그림(별도 CDN 라이브러리 추가 안 함). ②분기별 목표(빨강)·실적(파랑) 막대 + 달성률 % 라벨(100%↑ 초록/80~100% 노랑/80%↓ 빨강). ③누적 실적(파랑 실선) vs 누적 목표(빨간 점선) — 예상월로 이어지는 구간만 Chart.js `segment.borderDash`로 점선 처리.
- **요약카드 6개**: 연간 목표/누적 실적/연간 달성률/상반기 달성률/당월 예상액/잔여 필요액 (모두 억원, 소수 1자리).
- **단위 처리**: 목표 상수(백만원, ÷100=억)와 엑셀 실적(원, ÷100,000,000=억)을 명확히 분리해 변환.
- **예외 처리**: 요약표를 못 찾으면 주황 경고, 1팀 필터 결과 0건이면 "해당 조건의 매출 데이터가 없습니다" 표시, 숫자 아닌 셀은 0 처리.

**검증**: 실제 구조를 흉내낸 합성 엑셀(본부 요약표 + 월별 실적행 + 분기소계행 + 매출예상행 + 1팀 상세 블록 + 팀 외 인원 데이터 + 빈 행)을 만들어 파싱 결과를 JSON으로 직접 확인, 본부/1팀 각각 요약카드 수치를 손으로 재계산해 일치 확인, 요약표 없음/1팀 데이터 없음 예외 상황, 시트 자동탐지 3가지 케이스(이름매칭/폴백/시트1개뿐), 문자열·숫자 일련번호 두 가지 날짜 형식, 구간이동 세그먼트 점선 처리까지 브라우저에서 직접 실행해 확인. 콘솔 에러 없음.

#### 파이프라인 추이 탭: 개편중 처리
- 탭 이름을 "파이프라인 추이 (개편중)"으로 변경하고 회색(#5a5f78)으로 낮춰 다른 탭과 구분.
- 탭 클릭 시 기존 UI 대신 "개편 중입니다 / 현재 이 기능은 개선 작업이 진행 중입니다. 인벤토리 비교 탭을 이용해 주세요." 안내 화면만 표시.
- 기존 HTML은 삭제하지 않고 HTML 주석으로 보존, JS 함수(loadPipelineData/savePipelineData/renderPipelineChart 등)도 그대로 남겨둠 — 추후 복구 시 안내 블록 제거 + 주석 해제 + switchTab()에 renderPipelineChart() 호출만 되살리면 됨.
- getTargetForWeek()는 매출 추이 개편으로 완전히 대체되어 삭제(파이프라인 추이와는 무관한 함수였음).

---

### 2026-08-03 — 매출 추이 차트 구성 변경 (5차 작업, 4번째 세션 내 후속)

기존 [월별 실적 vs 목표]/[분기별 달성률]/[누적 실적 vs 누적 목표] 3종 차트를 아래로 교체.
데이터 파싱 로직(엑셀 시트 탐지, 본부 요약표/1팀 상세블록 파싱, 예상액 처리)은 변경 없음.

- **월별 개별 목표는 화면에 표시하지 않음** — 분기 목표를 3등분한 값은 누적선을 만드는 데만 내부적으로 사용(`monthlyTargetRef` → `cumulativeTargetAt`). 3·6·9·12월의 누적 목표는 수학적으로 항상 실제 분기 누적 목표와 정확히 일치(3등분 합 = 원래 값이므로 당연히 일치 — 코드로 직접 검증함).
- **[차트1] 누적 실적 vs 누적 목표**(메인): 누적목표 빨강(#e74c3c) 실선 vs 누적실적 파랑(#4a9eff) 실선+반투명 채우기. 분기 경계월(3·6·9·12)은 포인트를 크게(6px), 그 외는 3px. 실적 없는 미래월은 null로 끊어서 0으로 안 떨어지게 처리, 예상월로 이어지는 구간만 점선. 툴팁에 격차/달성률을 함께 표시(`afterBody`).
- **[차트2] 누적 달성률 추이**: 누적실적÷누적목표×100을 월별로 표시, 100% 기준 회색 점선, 선 색상은 Chart.js `segment.borderColor`로 세그먼트별 100% 기준 초록/주황 분기.
- **[차트3] 분기별 목표 대비 실적**: 목표(빨강)·실적(파랑) 막대 + 달성률 % 라벨(기존 유지) — 실적 데이터가 하나도 없는 미래 분기는 막대 자체를 안 그리도록(`null`) 수정. 부분적으로 데이터가 있는 분기(진행 중인 분기)는 있는 만큼만 합산해서 표시.
- **[차트4] 항목별 누적 구성**(신규): 제품/상품/용역/유지보수 4개 항목의 월별 누적을 Chart.js `scales.y.stacked:true` 기반 누적 영역 그래프로 표시. 색상 고정(제품=파랑/상품=초록/용역=주황/유지보수=보라).
- **요약카드 6개 교체**: 연간 목표 / 누적 실적 / 현재 시점 누적 목표(신규) / 목표 대비 격차(신규, 초과 초록 +·미달 빨강 -) / 누적 달성률(마지막 실측월 기준 누적목표 대비, 기존 "연간 달성률"과는 다른 값) / 잔여 필요액. 기존 "당월 예상액"·"상반기 달성률" 카드는 제거.
- 더 이상 쓰이지 않게 된 막대-값-라벨 플러그인(`salesValueLabelPlugin`)은 삭제.

**검증**: 기존 합성 테스트 엑셀로 요약카드 6개 수치를 손으로 재계산해 전부 일치 확인(현재시점누적목표·격차·누적달성률 포함), 분기 경계 4개월의 누적목표가 실제 분기 목표와 소수점까지 정확히 일치하는지 코드로 직접 확인, 포인트 반지름 배열이 분기 경계만 크게 나오는지 확인, 차트2 세그먼트 색상 함수를 양쪽 케이스(100 이상/미만)로 직접 호출해 확인, 차트3에서 데이터 없는 미래 분기(Q4)가 null로 나오고 부분 데이터 분기(Q3)는 있는 만큼 합산되는지 확인, 차트4의 4개 항목 누적 합이 차트1의 누적실적 총액과 마지막 실측월 기준으로 정확히 일치하는지 확인. 본부/1팀 스코프 전환도 재확인. 콘솔 에러 없음.

---

### 2026-08-03 — 매출 추이에 파이프라인 전망 반영 + 파이프라인 추이 탭 제거 (6차 작업)

#### 파이프라인 추이 탭 제거
- 탭 버튼과 `tab-pipeline-trend` 콘텐츠 영역(안내 placeholder 포함)을 HTML에서 완전히 삭제. 남는 탭: 인벤토리 비교 / 매출 비교 / 변동보고서 / 매출 추이.
- 관련 JS 함수(`ALL_WEEKS`, `pipelineChart`, `loadPipelineData`/`savePipelineData`/`clearPipelineData`/`togglePipelineInput`/`renderPipelineChart`)는 삭제하지 않고 `/* ... */` 블록 주석으로 통째로 보존 — 복구 시 주석만 해제하면 되도록 함. 더 이상 참조되지 않는 `.tab-disabled`, `.trend-placeholder*` CSS는 삭제.

#### 매출 추이: 목표 대비 누적 실적 + 파이프라인 전망 차트로 재개편
직전(5차) 작업의 4종 차트(누적실적/달성률추이/분기별실적/항목별누적)를 전부 걷어내고, 인벤토리 비교 탭과 연동되는 새 메인 차트 1개 + 보조 차트 1개로 교체. 목표 상수·엑셀 파싱 로직(시트탐지, 본부요약표, 1팀상세블록, 예상액)은 변경 없음.

- **인벤토리 연동**: `getPipelineBySectionEok(scope)` — `INV_RESULT`(인벤토리 비교 탭에서 분석 완료된 결과)가 없으면 `null` 반환. 있으면 `INV_FILES.curr`의 100/90/75/50%(F) 4구간 `총합계`를 합산(백만원→억은 ÷100). 1팀 스코프일 때는 인벤토리 행의 `영업담당자`가 1팀 3명(공백 제거 후 비교)인 행만 합산 — 매출 실적 필터와 동일한 명단 재사용.
- **메인 차트(3선)**: ①누적 실적(파랑 #2a78d6 실선, 실적 없는 미래월은 null로 끊음) ②파이프라인 반영 전망(초록 #1baf7a 점선 — 마지막 실적월의 누적값과 동일한 지점에서 시작해 잔여 월에 파이프라인 금액을 균등 배분·누적) ③누적 목표(빨강 #e34948 얇은 실선 1.5px, 점 없음 — 실적선(2px)보다 얇게 해서 "확정 기준선"임을 시각적으로 구분). Chart.js 기본 범례는 끄고(`legend:{display:false}`) HTML로 직접 구성해 차트 위에 배치.
- **각주 동적 처리**: "N월 이후 초록 구간은…" 문구의 월(N)은 하드코딩하지 않고 마지막 실적월+1을 실제로 계산해서 삽입(`pipelineStartMonth = lastIdx + 2`) — 매주 파일이 바뀌어도 문구가 항상 정확하도록 함.
- **보조 차트**: 100%/90%/75%/50%(F) 4구간 파이프라인 구성을 가로 누적 막대 1개로 표시(`indexAxis:'y'`, `stacked:true`), y축 라벨에 총액 병기("파이프라인 62.4억" 형식), 범례는 HTML로 구성해 구간별 금액을 함께 표기.
- **요약카드 6개 전면 교체**: 연간 목표 / 누적 실적 / 파이프라인(50%F 이상) / 실적+파이프라인 합계 / 목표 대비 부족액(항상 빨간색) / 누적 달성률. 기존 "현재 시점 누적 목표"·"목표 대비 격차" 카드는 제거(누적 달성률 계산에는 내부적으로 여전히 사용).
- **예외 처리**: 인벤토리 미분석 시 두 차트 영역 모두에 "인벤토리 비교 탭에서 먼저 분석하면 파이프라인 전망이 함께 표시됩니다" 회색 안내 표시, 파이프라인 데이터셋/차트 자체를 그리지 않음. 기존 요약표 미탐지·1팀 데이터 0건 예외 처리는 유지.
- 더 이상 쓰이지 않게 된 `buildCumulativeByCategory`, `buildRatioLabelPlugin`, `quarterMonths`, 그리고 4종 차트 렌더 함수는 삭제.

**검증**: 인벤토리 비교 탭의 기존 테스트 픽스처(100/90/50%(F)/25% curr 파일)를 실제로 로드해 `runInvCompare()` 실행 후, 각 구간 `총합계`를 직접 합산한 수동 계산값과 `getPipelineEok`/`getPipelineBySectionEok` 반환값이 정확히 일치하는지 확인(70.188억). 인벤토리 미분석 상태에서 두 안내 문구가 모두 뜨는지, 분석 후 사라지는지 확인. 합성 행에 1팀 소속 영업대표를 추가해 본부 스코프(필터 없음)와 1팀 스코프(필터 있음) 결과가 예상대로 달라지는지 직접 검증. 각주의 "N월 이후" 문구가 마지막 실적월에 따라 동적으로 바뀌는지 확인(테스트 데이터 기준 "9월 이후"로 정확히 계산됨). 요약카드 6개 수치를 손으로 재계산해 전부 일치 확인. 콘솔 에러 없음.

---

### 2026-08-03 — 파이프라인 계산 개선: 100% 잔여분 반영 (7차 작업)

#### 목표
파이프라인 합계에 100% 구간 전액을 그대로 더하면 이미 매출로 청구된 금액까지 중복 집계되는 문제가 있어,
100% 구간은 "수주금액 - 이미 청구된 매출 실적 = 미청구 잔여분"만 반영하도록 계산 방식을 수정.

#### 구현 내용
- **매칭 로직**: 매출 시트 하단 상세 블록(`SALES_PARSED.teamRows` — "NO" 헤더 아래 실제 청구 라인만 존재하므로 "매출 예상액" 행은 애초에 섞이지 않음)에서 프로젝트코드별 누적 청구액을 합산(`getInvoicedAmountByProjectCode`). 인벤토리 100% 구간의 각 프로젝트코드와 매칭해 `잔여분 = max(0, 수주금액 - 청구액)`을 프로젝트 단위로 계산(음수 방지는 프로젝트별로 적용 — 여러 프로젝트를 합산한 뒤 한번에 클램프하면 초과청구 건이 미청구 건의 잔여분을 상쇄해버리는 오류가 생기므로 반드시 건별로 처리).
- **매칭 실패 처리**: 프로젝트코드를 못 찾으면 차감 없이 수주금액 전액을 잔여분으로 산입. 실패 건수가 있으면 차트 아래 회색 안내("매칭되지 않은 100% 프로젝트 N건...") + `[상세보기]` 토글로 프로젝트코드/프로젝트명/수주금액 목록을 펼쳐볼 수 있게 함.
- **90%/75%/50%(F)**: 기존과 동일하게 수주금액 전액을 그대로 합산(가중치 없음).
- **1팀 스코프**: 인벤토리 100% 행의 영업담당자로 먼저 필터링한 뒤 매칭 로직을 적용 — 필터로 제외된 프로젝트(팀 외 인원)는 "매칭 실패"로 집계되지 않고 애초에 계산 대상에서 빠짐(팀1 전용 미매칭 목록이 본부 스코프보다 항목이 적어야 정상).
- **목표 대비 부족액/초과액 부호 수정**: `부족액 = 연간목표 - (누적실적+파이프라인)`. 값이 양수면 "목표 대비 부족액"(빨강), 음수면 라벨을 "목표 대비 초과액"으로 바꾸고 절대값을 초록색으로 표시(더 이상 음수 그대로 노출하지 않음).
- **요약카드**: "파이프라인" 카드 라벨을 "파이프라인(100% 잔여 + 90/75/50F)"로 변경, 카드 안에 작은 글씨로 "100% 잔여 XX억 · 90/75/50F XX억" 내역을 추가로 표시.
- **보조 차트**: 확도별 구성 막대의 "100%" 항목명을 "100% 잔여"로 변경(값 자체도 잔여분으로 교체), 색상·범례 구조는 유지.
- **각주**: 파이프라인 계산 방식을 설명하는 각주 2개를 새로 추가(계산 방식 설명, 수주금액 기준 명시), 기존 "N월 이후..." 각주는 "50%(F) 이상" 문구를 빼고 "실제 매출 청구 시점 및 연도와 다를 수 있습니다"로 보강. 목표 관련 각주는 그대로 유지.
- **콘솔 로그**: `computePipelineData` 실행 시마다 100% 수주금액 합계·차감된 매출 합계·잔여분·매칭 성공/실패 건수·구간별 합계·파이프라인 최종 합계를 `console.log`로 출력(개발 확인용).

**검증**: 인벤토리 100% 프로젝트 4건(부분청구/전액초과청구/매칭실패/팀외인원)과 매출 상세행 2건으로 합성 픽스처를 만들어 `computePipelineData` 결과를 직접 확인 — 부분청구 건(수주 2000백만-청구 700백만=잔여 1300백만), 초과청구 건(수주 1000백만-청구 1200백만→잔여 0으로 클램프), 매칭실패 건(수주 600백만 전액), 팀외 인원 건(본부 스코프에서만 포함) 네 가지 케이스 전부 손계산과 정확히 일치 확인(본부 27.6억/1팀 23.6억). `[상세보기]` 토글 클릭 시 목록이 펼쳐지고 다시 접히는지, 매칭 실패가 0건일 때 안내가 자동으로 숨는지 확인. 목표 초과 시나리오를 인위적으로 만들어 "목표 대비 초과액" 라벨+초록색 전환이 정확히 동작하는지 확인. 콘솔에 진단 로그가 정상 출력되는지 확인. 콘솔 에러 없음.

---

### 2026-08-03 — 인벤토리 비교 구간별 항목 변동폭 표시 추가 (8차 작업)

#### 목표
구간별 요약 영역에 총합계 변동만 보이던 것을, 제품/상품/용역/유지보수 항목별 변동폭까지 한눈에 보이도록 추가.

#### 구현 내용
- **공용 계산 함수**: `buildCatDiffData(sectionId)` — 제품/상품/용역/유지보수/총합계 5개 항목에 대해 전주(또는 N주 전)·금주 합계와 diff, 변동없음 여부(0.05백만 미만)를 계산. 화면(`buildInvCategoryDiffLineHtml`)·보고문 초안·엑셀 다운로드가 모두 이 함수 하나를 공유해서 계산 로직이 세 곳에서 어긋나지 않도록 함.
- **화면**: 기존 "이전 회차 대비 ▼918.8백만 | ..." 총합계 줄 바로 아래에 `.inv-cat-diff-line`(flex-wrap, gap 24px)으로 항목별 줄 추가. 증가=초록 ▲, 감소=빨강 ▼, 무변동=회색 "─"(금액 생략). 화살표가 방향을 나타내므로 숫자에 +/- 중복 부호를 붙이지 않도록 `fmtAmt(Math.abs(diff))` 사용.
- **엑셀 다운로드**: 요약 문구 다음 줄에 ExcelJS `richText`로 항목별 줄을 추가(항목명은 회색, 증가는 초록 굵게, 감소는 빨강 굵게, 무변동은 회색 "─") — 화면과 동일한 색상 규칙을 그대로 재현.
- **보고문 초안**: `[90%] 이전 회차 대비 ▼918.8백만` 헤더 바로 다음 줄에 들여쓰기(공백 6칸)로 `제품 ▼93.1 / 상품 ─ / 용역 ▲53.4 / 유지보수 ─`처럼 단위(백만) 없이 축약 표기 — 화면·엑셀과 달리 report 텍스트는 컴팩트하게 유지. "변동 없음" 헤더로 조기 반환되는 구간에도 항목별 줄을 함께 붙이도록 함(총합계는 안 바뀌어도 항목 간 상쇄가 있을 수 있으므로).

#### 검증
- 초기 테스트에서 총합계 diff와 4개 항목 diff 합이 17.8백만 어긋나는 현상을 발견 → 원인 추적 결과 몇 세션 전에 수기로 만든 테스트 픽스처(OP-26-0334)의 총합계(2017.8)가 애초에 제품+상품+용역+유지보수(2000)와 일치하지 않게 잘못 입력되어 있던 것으로, 코드 버그가 아니라 오래된 테스트 데이터 자체의 결함이었음을 확인.
- 총합계=제품+상품+용역+유지보수가 정확히 성립하는 새 합성 픽스처(제품 100→6.9, 상품 200→200, 용역 300→353.4, 유지보수 5→5)로 재검증 → 항목별 diff 합(-39.7)과 총합계 diff(-39.7)가 정확히 일치, 화면 표시("제품 ▼93.1백만 상품 ─ 용역 ▲53.4백만 유지보수 ─ 총합계 ▼39.7백만")·보고문 초안·엑셀 richText 색상까지 전부 요청 형식과 일치함을 직접 확인. 콘솔 에러 없음.

---

### 2026-08-05 — 보고문 초안 양식 개편 (9차 작업)

#### 목표
기존 `[90%] 이전 회차 대비 ▼918.8백만` + 항목 나열 형태를, "-------- 사업기회 현황" 제목 아래
구간별 번호(1./2./3...)와 구간 내 분류별 하위번호(2.1./2.2...)를 매기는 정식 보고서 양식으로 전면 교체.
기존 항목별 카테고리 변동폭 줄(제품/상품/용역/유지보수)은 화면·엑셀에는 그대로 유지하고 보고문 초안에서만 제거(새 양식에 해당 항목이 없음).

#### 구현 내용
- **분류 5종**: 유입(다른 구간에서 이동) / 신규등록 / 금액변동(방향 혼재 시 상승·감소 분리) / 유출(목적지 구간별로 소제목 분리) / 완전삭제. 완전삭제는 스펙에 명시되어 있지 않았으나, 사용자에게 확인 후 5번째 분류로 추가하기로 결정(빠지면 인벤토리에서 아예 사라진 항목이 보고서에서 누락됨).
- **데이터 소스**: `INV_RESULT.bySection[id]`의 기존 `inflow`/`brandNew`/`outflow`/`fullyDeleted` 배열과 `rows.filter(type==='changed')`를 그대로 재사용 — 별도 재계산 없이 구조만 재배열.
- **금액 표기**: 소수 첫째 자리가 0이면 생략(`fmtAmtReport`), 증가는 `▲금액`(부호 없음), 감소는 `▼-금액`(마이너스 명시) — 기존 `fmtAmt`(항상 소수 1자리, 부호 없음)와 다른 새 포맷 함수를 report 전용으로 추가.
- **구간이동 항목 금액 산정**: 유입 항목은 도착 구간 기준 `currAmt` 전액을 `▲`로, 유출 항목은 출발 구간 기준 `prevAmt` 전액을 `▼-`로 표기 — 항목 자체의 `diff`가 아니라 "이 구간 합계에 미친 영향"(이동으로 인해 그 구간에 없던 게 생기거나, 있던 게 없어짐)을 기준으로 함.
- **소제목/들여쓰기**: 분류가 2개 이상이면 `N.N. 소제목` 줄을 공백 2칸으로, 그 아래 항목은 공백 4칸으로. 분류가 1개뿐이면 소제목 생략하고 항목을 공백 2칸으로 바로 나열.
- **비교기준일**: `getWeekDateInfo('prev').date.raw`(파일명에서 인식한 MMDD)를 우선 사용, 인식 실패 시 `invPrevLabel()`("전주"/"2주 전"/"이전 회차")로 폴백.
- **구간 포함 여부**: `INV_FILES.prev[id]`나 `curr[id]` 중 하나라도 존재하는 구간만 보고서에 포함(파일을 아예 안 올린 구간은 번호도 안 붙고 통째로 생략). 포함된 구간에 한해 1부터 순서대로 번호 부여.
- **정렬**: 각 분류 내에서 금액 절대값 내림차순.
- 더 이상 쓰이지 않게 된 `catDiffItemText`(구 보고문 전용 포맷터)는 삭제, `buildCatDiffData`는 화면·엑셀에서 계속 사용 중이라 유지.

#### 검증
- 유입 1건·신규등록 1건·금액변동 상승/감소 각 1건·유출 2건(서로 다른 목적지)·완전삭제 1건이 한 구간에 모두 발생하는 합성 픽스처를 만들어 전체 출력을 직접 확인 — 구간 헤더 금액, 분류별 소제목·번호, 항목별 금액과 이동경로 괄호 표기까지 전부 손계산과 정확히 일치(예: 90% 구간 헤더 `▲300백만`, 유입 `▲1,000백만`, 유출 각각 `▼-600백만`/`▼-300백만`). 단일 분류만 있는 구간(100%/50%(F)/0%)에서 소제목 없이 2칸 들여쓰기로 나오는지 확인. 완전삭제 2건을 추가해 금액 큰 순 정렬이 맞는지 확인(5,000백만이 100백만보다 먼저). 파일을 안 올린 75%/25% 구간이 보고서에서 완전히 빠지는지 확인. 등폭 폰트 렌더링 확인. 콘솔 에러 없음.
  - 테스트 중 전체 항목이 `▼-NaN백만`으로 깨지는 문제가 있었으나, 원인은 테스트 픽스처 생성 스크립트에서 헬퍼 함수에 인자를 하나 더 넘겨 값이 밀린 것으로 확인 — 코드 버그 아님.

---

### 2026-08-05 — 매출 예상 수기 입력 기능 추가, 파이프라인 전망 산출 방식 변경 (10차 작업)

#### 목표
파이프라인 전망을 "잔여 월 균등 배분" 추정치에서 사업별 수기 입력 기반으로 전환.
매출 추이 탭에 "매출 예상 입력" 표를 추가해 사업별로 매출예상월·예상금액을 직접 입력하고,
차트/요약카드는 이 입력값만을 반영(입력 없는 사업은 추정하지 않음).

#### 구현 내용
- **CDN 추가**: idb-keyval@6 UMD(`window.idbKeyval`)로 IndexedDB 저장. 저장 키 `'revenue_forecast'`, 구조는 `{프로젝트코드: [{월,금액}, ...]}`. 입력 변경(`onchange`) 즉시 자동 저장, 저장 버튼 없음. 인벤토리에서 사라진 사업의 입력값도 삭제하지 않고 그대로 보관(코드 기준 저장이라 자연히 유지됨).
- **인벤토리 파서 확장**: `parseInventorySheet`에 컬럼명 동시-포함 탐지(`findColBoth`)를 추가해 "수주"+"예정" → 수주예정일, "매출"+"예정" → 매출예정일 컬럼을 동적으로 찾아 원본 셀 값(Date/일련번호/문자열 무엇이든) 그대로 저장. 표시 시점에 `parseExcelDateFull`(Date 객체/엑셀 일련번호/`2026-12-31`/`26.12.31`/`2026년 12월` 세 문자열 포맷 모두 처리)로 변환 후 `formatYM`으로 "26.09" 형식 표기.
- **후보 목록 공용화**: 기존 `computePipelineData`(집계만 반환)를 `getPipelineCandidateRows(scope)`(사업별 원본 행 배열 반환)로 리팩터링 — 100% 구간은 매출 실적 매칭 후 잔여분이 0보다 큰 건만, 90/75/50%(F)는 전액으로 포함. 매출 예상 입력 표·차트·요약카드가 모두 이 목록 하나를 공유.
- **`computeForecastSummary(scope)`**: 후보 목록에 `REVENUE_FORECAST`를 조인해 월별 합계(2026년만 차트 X축에 반영)·확도 구간별 합계·입력완료/미입력 건수 및 금액을 한 번에 계산.
- **메인 차트 파이프라인선**: 마지막 실적월의 누적값에서 시작해, 그 이후 달은 실제로 입력된 그 달의 예상금액만 누적(균등 배분 로직 완전 삭제). 입력이 하나도 없으면 선 자체를 그리지 않고 "매출 예상을 입력하면 파이프라인 전망이 표시됩니다" 안내로 전환(인벤토리 미분석 시의 기존 안내와는 별도 문구).
- **보조 차트(확도별 구성)**도 인벤토리 수주금액이 아니라 입력된 예상금액을 사업의 확도 구간별로 집계하도록 변경 — 탭 전체에서 "파이프라인" 수치가 항상 입력 기준으로 일관되게 함.
- **요약카드**: "파이프라인" 카드가 입력된 예상금액 합계(올해분)로 교체, 작은 글씨로 "입력 N건 · 미입력 N건(XX억)" 표기. 미입력 금액은 어떤 합계에도 포함하지 않음.
- **매출 예상 입력 표**: 업로드 영역 바로 아래, 인벤토리 분석 결과만 있으면 매출 엑셀 업로드 여부와 무관하게 독립적으로 동작(엑셀 업로드가 필요한 요약카드·차트 영역과는 별도 게이팅). 접기/펼치기, 상단 요약 바(전체/입력완료/미입력 건수, 입력 합계, 미입력 수주금액 합계), 필터(전체/미입력만/입력완료 + 확도별) + 검색 + 컬럼 헤더 클릭 정렬(기본: 수주금액 내림차순), 사업 1건당 여러 입력 행(rowspan 병합 + `[+]`/`[−]`), `[매출예정일로 일괄 채우기]`(2026년 매출예정일이 있고 미입력인 건만, 확인창 후 실행, 기존 입력값은 건드리지 않음), `[입력값 백업]`/`[입력값 복원]`(JSON 파일).
- **각주 교체**: 기존 "N월 이후 균등 배분" 각주를 삭제하고 "파이프라인 전망은 사업별로 직접 입력한 매출 예상월·예상금액을 합산한 값입니다" / "매출예정일이 2027년 이후인 사업은 올해 전망에서 제외됩니다" 두 개로 교체. 100%-잔여분 계산 방식 각주는 유지.

#### 검증
- 100% 구간 3건(부분청구/매칭실패/전액청구)을 매출 상세행 2건과 매칭한 결과, 전액청구건(잔여 0)이 후보 목록에서 자동 제외되는지 직접 확인. 팀1 스코프에서 팀 외 인원 소유 사업이 후보 목록에서 빠지는지 확인.
- `parseExcelDateFull`을 세 가지 날짜 문자열 포맷(`2026-09-15`/`26.12.31`/문자열 없음)과 2027년 이후 매출예정일 케이스로 직접 호출해 파싱·강조색 처리 확인.
- 입력 후 IndexedDB에 정확한 구조로 저장되는지(`idbKeyval.get('revenue_forecast')`), 페이지를 완전히 새로고침한 뒤에도 `REVENUE_FORECAST`가 그대로 복원되는지 직접 확인.
- 인벤토리·매출 데이터를 다시 로드한 뒤 메인 차트의 파이프라인선이 입력된 달에만 계단식으로 증가하고(9월 +500백만, 12월 +300백만) 나머지 달은 값이 그대로 유지되는지(균등 배분이 전혀 발생하지 않는지) 확인. 입력이 0건일 때 두 차트 영역 모두 새 안내 문구로 전환되는지 확인.
- 필터(전체/미입력만/입력완료/확도별)·검색·정렬 토글이 각각 올바른 건수로 필터링되는지 확인.
- `[매출예정일로 일괄 채우기]`가 2026년 매출예정일이 있는 미입력 건만(2027년 건·매출예정일 없는 건 제외) 해당 월·수주금액 전액으로 채우는지 확인. 입력값 백업 → 초기화 → 복원 시 원래 값과 동일하게 되돌아오는지 확인. 입력 행 추가/삭제 시 rowspan과 저장 배열 길이가 올바르게 바뀌는지 확인. 콘솔 에러 없음.

---

### 2026-08-05 — 원본 비고 표시 및 검색 기능 추가 (11차 작업)

#### 목표
인벤토리 비교 탭 표의 "비고" 열이 시스템이 자동 생성한 변동 설명("90% → 100% 구간이동" 등)이라 영업담당자가 원본 엑셀에 직접 적어둔 메모를 볼 수 없었던 문제를 해결. 원본 비고 컬럼을 함께 읽어와 표시하고, 사업명·코드·고객사·담당자·비고 내용으로 전체 구간을 가로질러 검색할 수 있는 기능을 추가.

#### 구현 내용
- **원본 비고 컬럼 동적 탐지**: `parseInventorySheet`에서 `findCol([row2], '비고')`로 컬럼 인덱스를 파일마다 새로 탐지(고정 인덱스 금지, 100% 파일과 나머지 파일의 컬럼 구조가 달라도 각각 정상 인식). `normalizeCellText`로 `_x000D_`/`\r\n`/`\r`를 `\n` 하나로 정규화해 원본 행에 `'원본비고'` 필드로 저장, `runInvCompare`의 `item.note`로 전달.
- **표 컬럼 변경**: 순서를 `구분·고객사명·프로젝트코드·프로젝트명·영업담당자·수주확도·수주상태·변동내역·비고·제품·상품·용역·유지보수·총합계`로 변경. 기존 시스템 생성 "비고" 헤더는 "변동내역"으로 개명, 새 "비고" 열이 원본 텍스트를 담당. `remarkToHtml`로 `\n` → `<br>` 변환, `-webkit-line-clamp:2`로 2줄 말줄임 + `title` 속성 툴팁 + 클릭 시 `.expanded` 클래스 토글로 펼침.
- **엑셀 다운로드**: `ws.columns`/헤더 배열/데이터 셀 인덱스를 14열 구조로 재정렬(비고는 9번째 셀, `alignment:{wrapText:true, vertical:'top'}`), 카테고리·총합계 셀 인덱스와 합계행 병합 범위(`A:N`)도 함께 이동.
- **검색 UI**: 구간 탭 버튼 아래·요약 영역 위에 검색창(입력 즉시 필터링, 버튼 없음) + 검색 범위 토글([전체 구간]/[현재 구간], 기본값 전체 구간) + 구분 필터([전체]/[구간이동]/[신규]/[금액변동]/[삭제]/[유지], 기존 `.filter-bar`/`.filter-btn` 재사용) + 우측 정렬 "[비고 있는 건만]" 토글을 배치. `switchInvSectionTab`에서 `INV_ACTIVE_SECTION_ID`를 추적하도록 변경해 "현재 구간" 검색 범위가 실제 활성 탭을 따라가게 함.
- **검색 로직**: `INV_RESULT.bySection[id].rows`(구간당 신규/구간이동/변동/유지/삭제 항목이 모두 들어있는 배열)를 검색 범위에 따라 전체 구간 또는 활성 구간만 모아, 프로젝트명·코드·고객사명·영업담당자·원본비고·`buildRemark()`(변동내역 텍스트) 대상 대소문자 무시 부분일치로 필터링. 구분 필터·비고 필터와 AND 조건으로 결합. 완전히 클라이언트 사이드로만 처리(외부 전송 없음).
- **하이라이트**: `invHighlight`가 원본 문자열에서 대소문자 무시로 일치 구간을 찾아 `escHtml`로 이스케이프한 텍스트 사이사이에 `<mark class="inv-search-hl">`로 감싸 삽입(정규식 이스케이프 문제를 피하기 위해 `indexOf` 기반 수동 탐색 방식 채택). 줄바꿈이 있는 비고는 `invHighlightMultiline`으로 하이라이트 후 `\n`→`<br>` 변환.
- **검색 결과 카드**: 검색어가 있으면 `#inv-section-panels`를 숨기고 카드 목록으로 교체(검색어를 지우면 다시 표로 복귀). 카드 구성은 배지 줄(구분 색상 배지·수주확도·프로젝트코드 파란 등폭)·프로젝트명·고객사/담당자/상태·변동내역 회색 박스·비고 노란 박스(없으면 "작성된 비고 없음")·금액표·이전 회차 대비 증감(▲초록/▼빨강/"변동 없음")순. 구분 배지 색상은 기존 `.badge-move`(보라)/`.badge-changed`(노랑)/`.badge-del`(주황)/`.badge-same`(회색)을 그대로 재사용하고, 신규만 스펙상 초록이 필요해 새 `.badge-search-new` 클래스를 추가(기존 `.badge-new`는 파란색이고 매출 비교 탭과 공유하는 클래스라 변경 불가).

#### 검증
- 비고 컬럼 위치가 서로 다른 두 가지 파일 구조(비고가 앞쪽/맨 뒤)로 합성 픽스처를 만들어 두 구조 모두에서 비고가 정확히 인식되는지 확인. `_x000D_` + 개행이 섞인 텍스트가 `\n` 하나로 정규화되고 화면에서 `<br>`로 정확히 변환되는지 확인.
- 신규/구간이동/금액변동/완전삭제/유지 5개 타입 모두에서 `item.note`가 올바르게 채워지는지 직접 확인(`INV_RESULT.bySection[id].rows`를 순회해 code/type/note 비교).
- 비고 셀의 `title` 툴팁과 클릭 시 `.expanded` 토글(펼침→접힘)이 정상 동작하는지 확인.
- 검색어 입력 시 전체 구간에서 다중 매치(사업명 부분일치)가 올바른 건수로 나오는지, 구분 필터를 AND로 걸었을 때 건수가 줄어드는지, 검색어를 지우면 원래 표(요약+구간이동내역+표)가 그대로 복귀하는지 확인. "현재 구간" 범위로 전환 시 활성 탭과 무관한 항목이 걸러지는지 확인(활성 탭 밖의 매칭 항목이 0건으로 정확히 빠짐).
- 검색 결과 카드의 배지 색상(신규=초록/구간이동=보라)과 하이라이트(`<mark>` 삽입) 렌더링을 스크린샷으로 직접 확인, 변동내역·비고 박스·금액표·증감 표시까지 스펙과 정확히 일치.
- 엑셀 다운로드 결과를 ExcelJS로 다시 읽어들여 헤더 순서(14열)·비고 컬럼 값·`wrapText:true` 속성이 정확히 반영됐는지 직접 확인. 콘솔 에러 없음.

---

### 2026-08-05 — 전체 탭 분석 결과 저장 및 마지막 탭 유지 기능 추가 (12차 작업)

#### 목표
인벤토리 비교 탭에만 있던 새로고침 후 상태 유지 기능을 매출 비교·매출 추이 탭까지 확장하고, 사용자가 마지막으로 보던 탭도 새로고침 후 그대로 열리도록 함.

#### 구현 내용
- **저장소**: idb-keyval(IndexedDB)을 매출 예상 입력 기능과 동일하게 재사용. 키는 탭별로 독립 — `sales_compare`(매출 비교), `sales_trend`(매출 추이), `last_active_tab`(마지막 탭). 인벤토리 비교 탭은 기존 localStorage(`inv_compare_files`) 방식을 그대로 유지(이미 정상 동작 중이라 변경하지 않음).
- **저장 대상**: 원본 File 객체/base64는 저장하지 않고, 업로드 시 파싱된 행 데이터(인벤토리 탭의 `INV_FILES.rows`와 동일한 성격)만 저장 — 매출 비교는 `DATA.sal.prev/curr`(파싱된 매출목록 행), 매출 추이는 `SALES_PARSED`(파싱된 실적/상세행 구조)를 그대로 저장하고, 새로고침 시 저장된 데이터로 비교/집계를 다시 실행해서 화면을 재구성. 파일명·분석 실행시각(`analyzedAt`)·요약 수치(`summary`)도 함께 저장. 매출 추이는 스코프(본부/1팀)와 월별 집계 시리즈(`monthlySeries`)도 별도 저장. 모든 저장 객체는 `roundNumbersDeep()` 공용 유틸로 숫자를 소수점 2자리로 반올림해서 저장.
- **변동보고서 탭**: 별도 저장 없음 — 인벤토리 비교 탭 데이터(`INV_RESULT`)만으로 완전히 파생되는 탭이라 인벤토리 복원 후 해당 탭을 열면(`generateReport()`) 자동으로 재현됨.
- **자동 저장**: 매출 비교는 `runCompare('sal')` 실행 직후, 매출 추이는 업로드 직후와 본부/1팀 스코프 토글 시마다 자동 저장(저장 버튼 없음).
- **자동 복원**: `DOMContentLoaded`에서 `loadRevenueForecast → restoreSalesCompareData → restoreSalesTrendData → restoreLastActiveTab` 순서로 순차 실행(매출 추이 복원이 매출 예상 입력값에 의존하므로 병렬 실행하지 않음). 각 복원 함수는 업로드 카드에 파일명을 되돌려놓고 "✓ 이전 업로드/분석 결과를 불러왔습니다. (분석: …)" 상태 문구를 표시.
- **마지막 탭 기억**: `switchTab()`이 호출될 때마다 `last_active_tab`에 현재 탭 id를 저장. 페이지 로드 시 저장된 값이 유효한 탭 id(`inventory`/`sales`/`report`/`revenue-trend`)이면 그 탭을, 아니면 인벤토리 탭을 기본으로 `switchTab()` 호출.
- **삭제 기능**: 매출 비교 `[초기화]`(`resetAll('sal')`)와 신규 추가한 매출 추이 `[초기화]`(`resetSalesTrend()`, 업로드 카드 아래 새로 배치)가 각각 자기 탭의 idb 키를 함께 삭제. 헤더 우측에 작은 회색 밑줄 텍스트 버튼 `[저장 데이터 전체 삭제]`(`clearAllStoredData()`)를 추가 — confirm 후 인벤토리·매출 비교·매출 추이·매출 예상 입력값·마지막 탭 기억까지 전부 삭제하고, 현재 보고서 탭이 열려 있어도 즉시 빈 상태로 갱신되도록 `resetInvAll()`에 `report-content` 초기화도 추가.
- **예외 처리**: 모든 저장/복원 함수를 try/catch로 감싸 IndexedDB 사용 불가 시 저장만 조용히 건너뛰고 분석 기능은 그대로 동작. 저장 데이터가 없거나 형식이 깨졌으면(`prev`/`curr`/`parsed` 필드 누락 등) 아무 것도 복원하지 않고 빈 상태로 시작.

#### 검증
- 매출 비교: 합성 매출목록 파일(신규/삭제/증가/감소 각 1건)을 업로드해 분석 실행 → idb `sales_compare`에 파일명·행 데이터·요약 수치가 정확히 저장되는지 확인 → 페이지를 완전히 새로고침한 뒤 파일명·행수·"변동이력 분석" 버튼 활성화 상태·요약 카드·표가 전부 그대로 복원되는지 직접 확인(재분석 결과 4건 타입 그대로 일치).
- 매출 추이: 합성 영업회의자료를 업로드하고 스코프를 1팀으로 전환 → idb `sales_trend`에 스코프·파일명·월별 시리즈·요약 수치가 저장되는지 확인 → 새로고침 후 스코프 토글(1팀 활성)·파일명·상태 문구·차트가 그대로 복원되는지 확인.
- 마지막 탭 기억: 매출 추이 탭에 머무른 상태로 새로고침 → 인벤토리가 아니라 매출 추이 탭이 자동으로 열리는지 확인.
- 초기화 버튼: 매출 비교/매출 추이 각각 `[초기화]` 클릭 시 해당 idb 키가 삭제되고 업로드 카드·상태 문구가 비워지는지 확인.
- 저장 데이터 전체 삭제: `confirm`/`alert`를 자동 승인 처리한 뒤 버튼 로직을 직접 호출해 `sales_compare`/`sales_trend`/`revenue_forecast`/`last_active_tab` idb 키와 인벤토리 localStorage가 전부 삭제되고, 보고서 탭이 즉시 안내 문구로 되돌아가는지 확인. 이후 새로고침 시 인벤토리 탭이 기본으로 열리는지 확인.
- 인벤토리 비교 탭 회귀 확인: 기존 픽스처로 업로드→분석→보고서 생성까지 정상 동작하는지, `resetInvAll()` 변경이 보고서 탭 초기화에만 영향을 주고 기존 동작을 깨지 않는지 확인. 전 과정에서 콘솔 에러 없음.

---

### 2026-08-05 — 비고 컬럼 파싱 수정 및 매출 목록 시트 자동 반영 (13차 작업)

#### 목표
실제 인벤토리 파일 구조(23개 컬럼, row[2] 헤더)를 사용자가 직접 확인해 제공 — 기존 코드가 이 구조와 어긋나 비고를 못 읽는 문제를 수정하고, 파일 두 번째 시트("매출 목록")에 이미 들어있는 매출 예상 데이터를 매출 추이 탭의 수기 입력 표에 자동으로 채워 넣는 기능을 추가.

#### 1. 인벤토리 목록 시트 파싱 수정
- `parseInventorySheet`가 더 이상 전체 시트를 순회하며 "파싱되는 시트를 아무거나" 쓰지 않고, 시트명에 "인벤토리"가 포함된 시트(없으면 index 0)를 직접 지정해서 읽도록 변경 — 두 번째 시트("매출 목록")를 잘못 인벤토리 데이터로 오인할 가능성을 원천 차단.
- 프로젝트코드 컬럼을 `findHeaderCol(row2, '프로젝트코드')`로 먼저 찾고(신규), 실패했을 때만 기존 'OP-' 셀 스캔으로 대체하도록 순서를 바꿈 — 기존에는 헤더 텍스트 검색이 아예 없어서 프로젝트코드 위치를 전적으로 'OP-' 스캔에만 의존했던 것이 근본 원인으로 추정됨.
- 비고를 포함한 모든 컬럼(고객사명/프로젝트명/영업담당자/수주확도/수주상태/제품·상품·용역·유지보수합계/총합계)은 계속 row[2]/row[3]/row[4] 헤더 텍스트로 동적 탐지(고정 인덱스 사용 안 함) — 실제 파일이 준 23개 컬럼 목록과 매칭 확인.
- **특수문자 깨짐 수정**: 파일 읽기 방식을 `FileReader.readAsBinaryString + XLSX.read(..., {type:'binary'})`에서 `readAsArrayBuffer + XLSX.read(new Uint8Array(...), {type:'array'})`로 전환 — binary-string 방식이 일부 유니코드 문자(➜ 등)를 손상시킬 수 있는 SheetJS의 알려진 한계였음. 같은 문제가 잠재된 매출 비교/매출 추이 탭의 파일 리더 2곳도 함께 동일하게 수정.
- 진단용 `console.log` 추가: 파일별 시트 목록, 인벤토리 목록 시트에서 찾은 전체 컬럼 인덱스(프로젝트코드·비고 포함).

#### 2. 매출 목록 시트 자동 반영
- `parseSalesListSheet(wb)` 신규 추가 — 시트명에 "매출"이 포함된 시트(없으면 index 1)를 읽어 row[2] 헤더로 프로젝트코드/매출순번/매출청구일/합계 컬럼을 탐지, row[4]부터 데이터 추출. 한 프로젝트가 여러 행(분할 청구, 매출순번으로 구분)일 수 있으므로 행 단위 그대로 반환.
- `loadInvSectionFile`에서 금주(curr) 파일을 로드할 때마다 이 시트도 함께 파싱해 `INV_FILES.curr[섹션].salesList`에 저장하고, `mergeAutoForecastFromInventory()`를 호출해 REVENUE_FORECAST(매출 예상 입력값)에 자동 반영.
- **자동/수기 구분 병합 로직** (`mergeForecastForCode`): 항목마다 매출순번을 `__seq`로 저장해 두고, 다음 주 재업로드 시 같은 `__seq`를 가진 기존 항목이 `auto:true`면 최신 값으로 갱신, `auto:false`(사용자가 수정함)면 절대 덮어쓰지 않고 그대로 보존. 프로젝트 전체가 이번 주 매출 목록에서 통째로 사라진 경우는 안전을 위해 자동 삭제하지 않음(7개 구간 파일을 한 번에 다 올리지 않는 실사용 패턴에서, 아직 안 올린 구간의 데이터를 잘못 지우는 사고를 방지하기 위한 의도적 설계).
- 표 UI: 자동 반영된 항목은 회색 배경(`.rev2-auto-cell`) + "자동" 배지(`.rev2-auto-badge`)로 표시, 사용자가 월/금액을 수정하면(`updateForecastEntry`) 그 즉시 `auto:false`로 전환되어 배지가 사라짐.
- 매출예상월 드롭다운(`buildMonthOptions`)을 2026년 전용에서 2026~2028년으로 확장 — 자동 반영된 항목의 매출청구일이 2027년 이후일 수 있어, 실제 값과 다른 옵션이 선택되는 버그를 방지.

#### 3. 매출 추이 탭 반영
- "매출예정일로 일괄 채우기" 버튼과 `bulkFillForecast()` 함수를 완전히 제거 — 이제 자동 반영이 업로드 시점에 항상 일어나므로 불필요.
- `computeForecastSummary`에 `autoCount`/`manualCount`(프로젝트 단위 — 수기 항목이 하나라도 있으면 수기입력으로 분류)와 `excludedCount`/`excludedMillion`(매출청구일이 2026년이 아닌, 주로 2027년 이후 항목의 건수·금액) 필드 추가.
- 매출 예상 입력 표 상단 요약을 "전체 N건 · 자동반영 N건 · 수기입력 N건 · 미입력 N건" 형식으로 변경.
- 메인 차트 아래 각주("※ 매출청구일이 2027년 이후인 사업은...")를 `excludedCount`가 있을 때 "N건(XX.X억)은 올해 전망에서 제외됩니다" 형태로 동적 표시하도록 변경.

#### 검증
- 실제 컬럼 구조(0~22 인벤토리 목록, 0~12 매출 목록)를 그대로 재현한 합성 픽스처로 직접 검증 — 최초 구현 시 `parseSalesListSheet`에서 `findHeaderCol(row2, ...)`를 배열이 아닌 단일 행으로 잘못 호출해(`[row2]`가 아닌 `row2`) 매출순번/매출청구일/합계가 전부 못 찾아지는 버그를 발견 → 수정 후 재검증하여 정상 동작 확인(콘솔 로그로 원인 즉시 특정 가능했음 — 진단 로그 요구사항이 실제로 유용했음을 확인).
- 비고 컬럼에 화살표(➜)와 개행이 섞인 텍스트가 파싱·화면 표시(`<br>` 변환)·엑셀 다운로드(ExcelJS로 재확인) 전 구간에서 깨지지 않고 그대로 보존되는지 직접 확인.
- 분할 청구(매출순번 1·2) 2건이 각각 별도 항목으로 자동 반영되는지, 프로젝트별 자동반영/수기입력/미입력 요약 카운트가 정확한지 확인.
- 매출청구일이 2027년인 항목이 파이프라인 차트 집계(2026년 누적)에서는 제외되면서도 표에는 정상 표시되고 각주에 "1건(3.0억)"처럼 정확히 반영되는지 확인.
- 핵심 시나리오 직접 검증: 항목을 수기로 수정(`updateForecastEntry`) → `auto:false` 전환 확인 → 같은 매출순번의 값이 바뀐 "다음 주" 파일을 재업로드 → 수기 수정 항목은 그대로 보존되고, 다른(여전히 auto인) 항목만 새 값으로 갱신되는지 직접 확인. 페이지 새로고침 후에도 이 상태(수기 999 보존 + 자동 항목 갱신값)가 그대로 유지되는지 확인.
- 매출 비교/매출 추이 탭 파일 리더도 ArrayBuffer 방식으로 정상 동작하는지 확인(기존 회귀 없음). "매출예정일로 일괄 채우기" 버튼이 UI에서 완전히 사라졌는지 확인. 전 과정에서 콘솔 에러 없음.

---

### 2026-08-05 — 매출 예상 자동 반영 수정 및 입력 섹션 위치 이동 (14차 작업)

#### 목표
직전(13차) 작업에서 추가한 "매출 목록 시트 자동 반영" 기능이 실제 파일에서 매출청구일은 읽지만 금액을 전혀 못 읽는 버그가 있어 수정. 동시에 "매출 예상 입력" 섹션을 차트 아래로 재배치하고 기본 접힘 상태로 변경.

#### 1. 배치 변경
- 매출 추이 탭 구조를 [본부/1팀 토글] → [파일 업로드] → [요약 카드] → [메인 차트] → [보조 차트] → [매출 예상 입력] 순서로 재배치(기존에는 업로드 바로 아래에 입력 섹션이 있어 차트보다 먼저 보였음). `#rev2-forecast-card` 블록을 `#rev2-content`(요약+차트) 다음으로 이동만 했을 뿐 내부 로직·id는 그대로라 별도 JS 변경 없음.
- `#rev2-forecast-body`에 `style="display:none"`을 기본값으로 추가하고 토글 아이콘 초기값을 `▸`로 변경해 기본 접힘 상태로 시작하도록 함(자동 반영이 정상 동작하면 펼쳐볼 일이 적다는 이유).
- 섹션 제목을 "매출 예상 입력" → "매출 예상 입력 (자동 반영 · 수정 가능)"으로 변경.

#### 2. 예상금액 자동 반영 버그 수정 (핵심)
- 원인: 실제 "매출 목록" 시트는 row[2] 인덱스 8이 "매출금액(백만원)"이라는 병합 헤더이고, 제품/상품/용역/유지보수/합계라는 실제 항목명은 그 아래 row[3]에 있음. 13차 작업의 `parseSalesListSheet`는 합계 컬럼을 row[2]에서만 찾아서 항상 못 찾았고(-1), 그 결과 모든 자동 반영 항목의 금액이 0으로 채워지면서 `Number(금액) > 0` 필터에 걸려 "입력 없음"으로 취급되어 왔음(매출예상월은 별도 컬럼이라 정상적으로 채워졌던 것).
- 수정: 합계 컬럼을 `findHeaderCol(row3, '합계')`로 찾도록 변경(못 찾으면 인덱스 12 폴백). 매출순번/매출청구일/프로젝트코드는 기존대로 row[2] 기준 유지.
- **숫자/빈칸 구분**: 셀 값이 숫자로 해석되지 않으면 빈칸(`''`)으로, 0이면 0 그대로 저장하도록 `Number()` 결과를 명시적으로 `isNaN` 체크(기존에는 `Number(v) || 0`이라 못 찾은 값과 진짜 0을 구분할 수 없었음). `mergeAutoForecastFromInventory`의 반올림 처리도 숫자일 때만 반올림하고 그 외엔 빈칸을 그대로 통과시키도록 함께 수정(안 그러면 `'' * 100`이 0으로 강제 변환되어 빈칸 구분이 다시 무너짐).
- 진단 로그 대폭 보강: 파일별 시트 목록, "매출" 시트 탐지 여부, row[2]·row[3] 전체 셀 값(인덱스 라벨 포함), 최종 컬럼 인덱스, 읽어온 첫 3건(코드/청구일/금액), 총 건수와 금액 합계.
- 파이프라인 전망 차트 각주 문구를 스펙 예시와 동일하게 "...제외되었습니다"(과거형)로 통일.

#### 3~4. 데이터 연동 / 차트 반영 확인
- 인벤토리 비교 탭에서 분석하지 않은 상태로 매출 추이 탭에 들어가면(`switchTab`) "인벤토리 비교 탭에서 먼저 분석해주세요" 안내가 정상 표시되는 기존 동작을 재확인(13차 작업에서 이미 구현됨, 이번엔 회귀만 검증).
- 매출청구일 2027년 이후 건이 파이프라인 차트(2026년 누적) 집계에서는 빠지고, 제외 건수·금액이 차트 아래 각주에 동적으로 표시되는 기존 로직도 재확인.

#### 검증
- 실제 파일과 동일하게 row[2] 인덱스8="매출금액(백만원)" 병합 헤더 + row[3]에 제품/상품/용역/유지보수/합계가 있는 합성 픽스처로 직접 검증 — 수정 전 코드로는 세 항목 모두 금액 0(또는 빈칸)이었던 것이, 수정 후 정확히 500/300/200으로 채워지는지 확인.
- 진단 로그가 스펙에 명시된 6가지 항목(시트 목록/시트 탐지 여부/row2·row3 셀 값/컬럼 인덱스/첫 3건/총계) 모두 콘솔에 정확히 출력되는지 확인.
- 상단 요약이 "전체 2건 · 자동반영 2건 · 수기입력 0건 · 미입력 0건 · 입력 합계 7.0억"으로 정확히 표시되는지 확인(500+200=700백만=7.0억, 2027년 건 300은 차트 집계에서만 제외되고 표에는 그대로 표시).
- 각주가 "※ 매출청구일이 2027년 이후인 1건(3.0억)은 올해 전망에서 제외되었습니다."로 스펙 예시 문구 형식과 정확히 일치하는지 확인.
- 자동 반영된 예상금액 입력 칸(회색 배경 `.rev2-auto-cell`)에 실제 값(500/300/200)이 채워져 표시되는지 직접 확인(수정 전에는 빈칸이었음).
- DOM 순서를 직접 조회해 `.trend-layout`의 자식이 [스코프 토글]→[업로드]→[경고]→[빈상태/컨텐츠(요약+차트2개)]→[매출 예상 입력] 순서인지, `#rev2-content` 내부가 [요약카드]→[메인차트]→[보조차트] 순서인지 확인. 페이지 로드 시 매출 예상 입력 섹션이 접힌 상태(`display:none`, 아이콘 `▸`)로 시작하는지 확인.
- 페이지 새로고침 후에도 자동 반영된 금액(500/300)이 그대로 유지되는지 확인. 전 과정에서 콘솔 에러 없음.

---

### 2026-08-05 — 검색 시 전체 현황 영역 유지되도록 수정 (15차 작업)

#### 목표
인벤토리 비교 탭에서 검색어를 입력하면 요약/구간이동내역/표/합계행이 통째로 사라지고 검색 결과 카드만 남아 바로 보고문 초안으로 이어지던 문제 수정. 검색은 조회 기능일 뿐 아래 전체 현황에 영향을 주면 안 됨.

#### 원인
`renderInvSearchResults()`가 검색어가 있을 때 `#inv-section-panels`(요약+구간이동내역+표+합계행이 들어있는 영역) 자체를 `display:none`으로 숨기고 검색 결과로 완전히 대체하고 있었음. `#inv-search-results`는 원래도 `#inv-section-panels` 앞에 위치해 있어서(DOM 순서 자체는 문제 없었음), 이 한 줄만 제거하면 자연스럽게 [검색 결과]→[요약/구간이동/표/합계행]→[보고문 초안] 순서가 되는 구조였음.

#### 수정 내용
- `panels.style.display = 'none'` 처리를 완전히 제거 — 검색 중에도 `#inv-section-panels`(현재 선택된 구간의 요약·항목별 변동폭·구간 이동 내역·표·금주 합계)가 항상 그대로 표시됨. 검색 필터는 `#inv-search-results` 영역에만 적용되고 아래 전체 현황에는 전혀 영향을 주지 않음(요구사항대로 완전히 분리된 별도 조회 기능으로 동작).
- 검색 결과 영역을 시각적으로 구분하기 위해 `.inv-search-results-wrap`으로 감싸고, 하단에 구분선(`.inv-search-divider`)과 회색 작은 글씨 "─ 전체 현황 ─"(`.inv-search-divider-label`)를 추가.
- 검색 결과 접기/펼치기 기능 추가: "검색 결과 N건" 제목 클릭 시 토글(`toggleInvSearchCollapse`), 기본은 펼침. `INV_SEARCH_COLLAPSED` 상태를 전역으로 유지해 검색어를 계속 수정하는 동안(재렌더링 반복)에도 접힘 상태가 풀리지 않도록 하고, 검색어를 완전히 지워 검색을 벗어나면 다음 검색은 항상 펼침 상태로 새로 시작하도록 초기화.

#### 검증
- 검색어 입력 후 `#inv-section-panels`의 `style.display`가 빈 문자열(표시 상태)로 유지되는지, 요약 문구·항목별 변동폭(`.inv-cat-diff-line`)·구간 이동 내역·`<table>`·`.inv-total-row`가 검색 중에도 전부 DOM에 그대로 존재하는지 직접 확인.
- `#inv-analysis-area`의 자식 순서를 직접 조회해 [구간탭]→[검색바]→[검색결과]→[구간패널(요약/구간이동/표/합계)]→[보고문 초안 박스] 순서가 스펙과 정확히 일치하는지 확인.
- 검색 중에도 현재 활성 구간 패널의 내용이 검색어와 무관하게 해당 구간의 전체 항목을 그대로 보여주는지(검색으로 필터링되지 않는지) 직접 확인.
- 검색 결과 헤더 클릭 시 본문이 접힘/펼침 전환되는지, 접힌 상태에서 검색어를 추가로 수정(재렌더링)해도 접힘 상태가 유지되는지, 검색어를 지운 뒤 다시 검색하면 펼침 상태로 초기화되는지 확인. 구분선과 "─ 전체 현황 ─" 라벨이 정확히 표시되는지 확인. 콘솔 에러 없음.

---

### 2026-08-06 — 1팀 인원 선택 체크박스 추가 (목표는 기본 3명 고정) (16차 작업)

#### 목표
매출 추이 탭의 "공공영업1팀" 스코프가 조영오·양소예·정인수 3명으로 하드코딩되어 있던 것을, 오수택·윤호영을 선택적으로 포함할 수 있게 확장. 단, 목표(SALES_TARGET_2026.팀1)는 항상 기본 3명 기준 고정값이라 추가 인원을 포함하면 달성률이 실제보다 높게 나오므로 이를 명확히 경고.

#### 구현 내용
- **인원 구조**: `TEAM1_BASE_MEMBERS`(조영오/양소예/정인수, 항상 포함) + `TEAM1_EXTRA_MEMBERS`(오수택/윤호영, 선택적) + `TEAM1_SELECTED_EXTRAS`(체크된 추가 인원) + `getTeam1ActiveMembers()`(base+선택 extras 합친 현재 집계 대상). 기존 하드코딩 상수 `SALES_TEAM1_REPS`는 완전히 제거하고 이 함수로 전면 대체.
- **동적 재계산으로 전환**: 기존에는 `parseSalesExcel()` 파일 업로드 시점에 `team1Months`/`team1Forecast`를 한 번만 계산해 캐시했는데, 이러면 체크박스를 바꿔도 반영이 안 됨. `parseSalesExcel()`은 이제 `teamRows`(원본 상세행)만 반환하고, 신규 `computeTeam1ScopeData(members)`가 `getScopeData()` 호출 시점마다 현재 선택된 인원 기준으로 매번 새로 집계하도록 변경. `aggregateTeam1ByMonth(rows, members)`도 인원 목록을 인자로 받도록 시그니처 변경.
- **목표는 항상 고정**: `annualTargetTotal`/`cumulativeTargetAt`은 `SALES_TARGET_2026[scope]`를 스코프 문자열('팀1')로만 조회하고 인원 목록을 전혀 참조하지 않으므로, 체크박스를 아무리 바꿔도 목표값은 원래부터 구조적으로 영향받지 않음(추가 코드 없이 기존 구조 자체가 요구사항을 만족).
- **파이프라인도 동일하게 동적화**: `getPipelineCandidateRows(scope)`의 팀1 필터링도 `getTeam1ActiveMembers()`를 사용하도록 변경 — 인벤토리 영업담당자가 체크된 인원에 포함된 건만 후보 목록(및 매출 예상 입력 표)에 반영.
- **UI**: 스코프 토글 바로 아래 `#rev2-team1-members` 영역(팀1 선택 시에만 표시) — 기본 3명은 체크·비활성 고정 + "(기본)" 회색 라벨, 추가 2명은 자유롭게 토글. 그 아래 "집계 대상: 조영오, 양소예, 정인수, 오수택 (4명) · 목표는 기본 3명 기준" 형식의 안내 줄(`renderTeam1MembersUI`가 매번 갱신).
- **달성률 왜곡 경고**: 추가 인원이 1명이라도 체크되면(스코프가 팀1일 때만) 요약 카드 아래(`#rev2-team1-distort-summary`)와 메인 차트 아래(`#rev2-team1-distort-chart`) 두 곳에 주황색 안내 박스 표시, "누적 달성률" 수치 옆에 작은 주황색 `*` 표시(`.rev2-distort-mark`) 추가. 체크 해제 시 즉시 숨김.
- **즉시 반영**: 체크박스 `onchange` → `toggleTeam1Extra()`가 `renderSalesTrend()`를 호출 — 요약카드·메인차트·보조차트·매출 예상 입력 목록이 한 번에 모두 재계산(이 함수가 내부적으로 네 가지를 전부 다시 그리는 기존 구조를 그대로 재사용).
- **저장/복원**: idb-keyval 키 `team1_members`에 `{scope, extras}` 저장. `toggleTeam1Extra`/`setSalesScope`에서 즉시 저장. 페이지 로드 시 `restoreSalesTrendData()`(기존에도 자체 스코프를 복원하던 것)보다 `restoreTeam1Members()`를 나중에 실행해 최종적으로 이 키의 값이 우선하도록 순서 조정, 이후 토글 버튼 클래스와 체크박스 UI·차트를 한 번 더 갱신.
- 매출 추이 탭 `[초기화]`(`resetSalesTrend`)는 파일 업로드 결과만 지우고 인원 선택은 유지(스코프는 본부로 리셋되며 자연히 체크박스 영역이 숨겨짐). 헤더의 `[저장 데이터 전체 삭제]`는 `TEAM1_SELECTED_EXTRAS`를 비우고 idb `team1_members` 키도 함께 삭제.

#### 검증
- 실적 상세행에 조영오/양소예/정인수 외에 오수택·윤호영·팀외인원(박다른팀) 행을 섞은 합성 엑셀로 검증 — 기본 상태(추가 인원 미체크)에서 누적 실적이 3명 합계(12.1억)와 정확히 일치, 연간 목표(290.6억)는 원래 값 그대로 유지되는지 확인.
- 오수택 체크 → 누적 실적이 22.1억(기존 12.1억 + 오수택 10억)으로 즉시 갱신, 목표는 290.6억 그대로, 달성률 뒤에 `*` 표시, 요약카드 아래·차트 아래 주황 경고 문구 모두 노출되는지 확인. 체크 해제 시 12.1억으로 되돌아가고 경고가 사라지는지 확인. 오수택 대신 윤호영을 체크해도 동일한 패턴(18.1억)으로 정확히 갱신되는지 확인.
- 인벤토리 파이프라인 후보 목록(`getPipelineCandidateRows('팀1')`)도 체크 전/후로 직접 호출해 비교 — 오수택 소유 프로젝트가 체크 전엔 후보에서 빠지고 체크 후엔 포함되는지 확인.
- 본부 스코프로 전환 시 인원 선택 영역이 완전히 숨겨지고 요약 수치가 인원 목록과 무관하게 그대로 나오는지 확인(회귀 없음).
- 체크박스 상태(윤호영 체크)와 스코프(팀1)를 idb에 저장한 뒤 페이지를 완전히 새로고침 — `SALES_SCOPE`/`TEAM1_SELECTED_EXTRAS`/마지막 탭/체크박스 UI/집계 대상 문구/요약 수치·경고 표시까지 전부 그대로 복원되는지 직접 확인. 콘솔 에러 없음.

---

### 2026-08-06 — 1팀 판별을 소속 기준으로 변경, 인원 체크박스 제거 (17차 작업)

#### 목표
16차 작업에서 추가한 "1팀 인원 선택 체크박스"를 완전히 제거하고, 1팀 판별 방식을 이름 하드코딩(`['조영오','양소예','정인수']`)에서 소속 컬럼("...클라우드공공영업1팀" 형태) 기준으로 전환. 인사이동·동명이인에 취약한 이름 매칭을 없애는 것이 목적.

#### 1. 인원 체크박스 기능 전체 제거
- HTML: `#rev2-team1-members`(체크박스 영역) 및 두 곳의 달성률 왜곡 경고 블록(`#rev2-team1-distort-summary`/`#rev2-team1-distort-chart`) 삭제.
- JS: `TEAM1_BASE_MEMBERS`/`TEAM1_EXTRA_MEMBERS`/`TEAM1_SELECTED_EXTRAS`/`getTeam1ActiveMembers`/`renderTeam1MembersUI`/`toggleTeam1Extra`/`saveTeam1Members`/`restoreTeam1Members` 전부 삭제. `computeTeam1ScopeData`/`aggregateTeam1ByMonth`는 유지하되 `members` 매개변수를 제거(더 이상 선택 인원 목록을 받지 않음).
- idb 키 `team1_members` 저장/복원/삭제 로직을 초기화 시퀀스·`clearAllStoredData`·`resetSalesTrend`에서 모두 제거.
- CSS: `.rev2-team1-members`/`.rev2-team1-checkboxes`/`.rev2-member-chip*`/`.rev2-team1-target-line`/`.rev2-team1-distort-notice`/`.rev2-distort-mark` 전부 삭제.

#### 2. 소속 기반 판별 도입
- `isTeam1Affiliation(raw)` — 소속 문자열의 공백(전각 포함)을 모두 제거하고 소문자로 정규화한 뒤 "공공영업1팀" 포함 여부로 판정. `normalizeAffiliation`이 정규화를 담당.
- `TEAM1_FALLBACK_NAMES = ['조영오','양소예','정인수']` — 소속 컬럼/값이 비어 있을 때만 쓰는 폴백 배열로 격하(더 이상 주 판별 수단이 아님). `isTeam1ByNameFallback(name)`으로 분리.
- **인벤토리 목록**: `parseInventorySheet`에 `findHeaderCol([row2], '소속')` 추가(고정 인덱스 아님), 파싱 결과 각 행에 `'소속'` 필드 저장. `getPipelineCandidateRows(scope)`의 `inScope`가 이제 각 인벤토리 행의 `소속`으로 직접 판정하고, 값이 비어 있을 때만 `영업담당자`로 폴백(이때 `TEAM1_AFFIL_FALLBACK_USED = true` 설정).
- **매출 목록 시트**: `parseSalesListSheet`에도 `findHeaderCol([row2], '소속')` 추가, 각 행에 `affiliation` 필드 저장. 프로젝트코드로 인벤토리를 매핑해 담당자를 조회하던 방식은 애초에 이 시트에 존재하지 않았음(코드 감사 결과 확인) — 요구사항대로 시트 자체의 소속 값만으로 직접 판정하도록 유지하고, 진단 로그(소속 고유 목록, 1팀 분류 건수)만 추가. REVENUE_FORECAST 병합 자체는 팀 구분 없이 전역으로 동작(기존과 동일) — 파이프라인 후보 필터링은 인벤토리 쪽에서 이미 처리되므로 중복 게이팅 없음.
- **매출 실적**(`SALES_PARSED.teamRows`, 주간 영업회의자료): 소속 컬럼이 없으므로 `buildInventoryAffiliationMap()`(전 구간 curr 인벤토리에서 프로젝트코드 → 소속 맵 구성)으로 프로젝트코드를 조회해 판정. 맵에 코드가 아예 없으면(매핑 실패) 1팀에서 제외 + 카운트, 코드가 있는데 소속 값만 비어 있으면 이름 폴백, 소속이 있으면 그 값으로 직접 판정. `aggregateTeam1ByMonth`/`computeTeam1ScopeData`(당월 예상액 재집계)에 동일하게 적용.
- 목표(`SALES_TARGET_2026.팀1`)는 스코프 문자열로만 조회되는 기존 구조 그대로라 이번 변경과 무관하게 항상 고정값 유지(코드 변경 없음).
- **폴백 UI 안내**: 렌더링마다 `TEAM1_AFFIL_FALLBACK_USED`를 리셋하고, 인벤토리 소속 판정 또는 매출 실적 소속 판정 중 하나라도 이름 폴백을 썼으면 스코프 토글 바로 아래 회색 문구 `#rev2-affil-fallback-notice`("소속 정보를 찾지 못해 담당자명 기준으로 분류되었습니다")를 노출. `updateAffilFallbackNotice()`가 `renderSalesTrend()`의 모든 반환 경로에서 호출되도록 함.

#### 3. 진단 로그
- 인벤토리/매출 목록 각각 소속 컬럼 탐지 인덱스와 소속 고유값 목록을 `console.log`.
- 인벤토리 1팀 분류(파이프라인 후보) 건수, 매출 목록 1팀 분류 건수, 매출 실적 1팀 분류/소속 매핑 실패 건수를 각각 `console.log`.

#### 검증
- 소속값이 "CEO > 영업부문 > OKC영업본부 > 클라우드공공영업1팀"/"...금융공공영업2팀"인 인벤토리 2건을 합성해 검증 — 구 하드코딩 명단에 없는 이름(김신입)이 소속만으로 1팀 후보에 포함되고, 명단에 있어도 소속이 다른 2팀 건은 제외되는지 직접 확인(이름 매칭이 실제로 더 이상 주 판별 수단이 아님을 증명).
- 소속 컬럼 자체가 없는 인벤토리 파일로 이름 폴백 경로를 검증 — 조영오(구 하드코딩 명단) 건이 정상적으로 폴백 판정되어 후보에 포함되고, 폴백 안내 문구가 표시되는지 확인. 소속이 정상 감지된 스코프에서는 안내가 숨는지 확인.
- 매출 목록 시트에 자체 소속 컬럼을 넣은 픽스처로 "1팀 분류: 1건/2건 중" 로그가 정확히 찍히는지 확인(인벤토리 매핑 없이 그 값만으로 판정).
- 주간 영업회의자료 매출 실적 3건(인벤토리 매핑+1팀, 인벤토리 매핑+2팀 제외, 인벤토리에 코드 없음=매핑 실패 제외)으로 검증 — 누적 실적이 1팀 매핑분(3.0억)만 정확히 반영되고, 콘솔에 "1팀 분류: 1건 / 소속 매핑 실패(제외): 1건"이 정확히 출력되는지 확인. 목표(290.6억)는 그대로 유지되는지 확인.
- 체크박스 UI(`#rev2-team1-members`)가 DOM에서 완전히 사라졌는지, `team1_members` idb 키가 더 이상 저장되지 않는지 직접 확인. 본부 스코프 전환 시 회귀 없는지, 새로고침 후 스코프·집계 결과가 그대로 유지되는지 확인. 콘솔 에러 없음.

---

### 2026-08-06 — 파이프라인 확도 구간 선택 및 소속 미분류 건 포함 옵션 추가 (18차 작업)

#### 목표
매출 추이 탭에 두 가지 독립적인 필터를 추가: ① 파이프라인 계산에 포함할 확도 구간(100%/90%/75%/50%(F))을 체크박스로 선택, ② 1팀 판별 시 소속에 팀 정보가 없는 "미분류" 건을 포함할지 선택. 두 설정 모두 즉시 재계산 + IndexedDB 저장.

#### A. 파이프라인 확도 구간 선택
- `PIPELINE_PROB_FILTER = {100,90,75,50f: true}` 전역 상태. `getPipelineCandidateRows(scope)`는 이 필터와 무관하게 팀 스코프만으로 전체 후보(`cand.rows`)를 반환하도록 유지하고, `computeForecastSummary(scope)`에서 `sectionAmounts`(필터 무관 구간별 전체 금액 — 체크박스 라벨용)를 먼저 계산한 뒤 `filteredRows = cand.rows.filter(r => PIPELINE_PROB_FILTER[r.section])`로 걸러 이후 모든 집계(월별 합계, 확도별 합계, 매출 예상 입력 목록의 `candidates`)에 사용 — 체크박스 라벨 금액은 필터와 무관하게 항상 전체 금액을 보여주고, 실제 반영 수치만 필터링되는 구조.
- 체크박스는 `renderProbFilterUI(sectionAmounts)`가 매 렌더링마다 다시 그림, `input[type=checkbox]`에 `accent-color: var(--chip-color)`로 확도별 색상(100%=#2a78d6, 90%=#1baf7a, 75%=#eda100, 50%(F)=#eb6834) 적용.
- 4개 모두 해제 시 `renderSalesChart1`/`renderPipelineCompositionChart` 둘 다 "포함할 구간을 하나 이상 선택해주세요" 안내로 전환(기존 "매출 예상을 입력하면..." 안내보다 우선순위 높음).
- 범례: `PROB_SHORT_LABELS`(100%잔여/90%/75%/50%F)를 선택된 구간만 `·`로 이어 붙여 "파이프라인 XX.X억 (90%·75%·50%F, 점선)" 형식으로 동적 생성.
- 주석: `buildPipelineSourceFootnote()`가 100% 포함 여부에 따라 "100% 구간의 미청구 잔여분과 90%·75%·50%(F) 구간의 수주금액을" / "90%·75%·50%(F) 구간의 수주금액을" 등으로 문구를 조합. 기존 "100% 구간은... 미청구 잔여분 기준" 각주는 100%가 체크된 경우에만 표시.
- 저장 키 `pipeline_prob_filter`(객체 그대로 저장).

#### B. 소속 미분류 건 포함 여부
- `classifyAffiliation(raw)` 신규 — 소속 문자열을 `'team1' | 'team2' | 'unassigned'`로 분류(공백 제거 후 "공공영업1팀"/"공공영업2팀" 포함 여부, 둘 다 없거나 빈 값이면 미분류). 17차 작업의 `isTeam1Affiliation`(boolean)과는 별개로 미분류를 구분해야 해서 새로 추가.
- **소속 컬럼 자체가 없는 파일(구조적 실패, 이름 폴백 대상)과 "컬럼은 있지만 이 행의 값에 팀 정보가 없음"(미분류)을 명확히 구분**하기 위해 `parseInventorySheet`가 각 행에 `__hasAffilCol`(그 파일에서 소속 컬럼을 찾았는지) 플래그를 추가로 저장. `getPipelineCandidateRows`/`aggregateTeam1ByMonth`/`computeTeam1ScopeData` 모두 `__hasAffilCol`이 false일 때만 이름 폴백을 쓰고, true인데 `classifyAffiliation`이 'unassigned'를 반환하면 `INCLUDE_UNASSIGNED` 전역 플래그(기본 true)에 따라 포함 여부를 결정.
- `getPipelineCandidateRows`는 100%/90%/75%/50%(F) 4개 구간을 스캔하며 미분류로 판정된 행을 `unassignedRows`(코드/이름/담당자/소속원본값/구간/금액)에 수집 — `INCLUDE_UNASSIGNED` 상태와 무관하게 항상 전체 수집(현재 제외돼 있어도 상세보기에서 확인 가능하도록). `buildInventoryAffiliationMap()`도 `{affil, hasCol}` 형태로 확장해 매출 실적 쪽 판정에서 동일한 구분을 재사용.
- 체크박스는 1팀 스코프 + 미분류 건이 1건 이상일 때만 표시(`renderUnassignedControl`), "(N건, XX억)" 병기, `[상세보기]` 클릭 시 프로젝트코드/프로젝트명/영업담당자/소속 원본값/금액 표를 펼침(접힘 상태는 재렌더링에도 유지).
- 체크 변경 시 `renderSalesTrend()` 전체 재실행 — 누적 실적/파이프라인/요약카드/매출 예상 입력 목록이 한 번에 갱신(17차 작업에서 이미 확보된 구조 재사용).
- 차트 아래 각주: 미분류 건이 포함된 상태(스코프=팀1 & INCLUDE_UNASSIGNED=true & 미분류 1건 이상)일 때만 "※ 소속이 팀 단위까지 입력되지 않은 N건(XX억)이 1팀 집계에 포함되어 있습니다." 표시.
- 저장 키 `include_unassigned`(boolean).

#### 공통
- 두 설정 모두 `renderSalesTrend()` 최상단에서 `computeForecastSummary(SALES_SCOPE)`를 한 번만 호출해 `forecastData`를 구하고, 이를 확도 체크박스·미분류 체크박스·요약카드·차트·매출 예상 목록이 전부 공유하도록 리팩터링(기존에는 성공 경로에서만 계산했음 — 이제 SALES_PARSED가 없어도(=매출 실적 파일 미업로드) 인벤토리 데이터만 있으면 두 체크박스 영역이 정상 동작).
- 진단 로그: `parseInventorySheet`에 1팀/2팀/미분류 건수, `getPipelineCandidateRows`에 미분류 건수+코드 목록, `computeForecastSummary`에 확도 구간별 금액을 각각 `console.log`로 추가(기존 소속 고유 목록 로그는 17차 작업에서 이미 존재).

#### 검증
- 100%(수주 1000백만, 잔여 전액)/90%(500백만)/75%(200백만)/50%(F) 2건(팀1 100백만 + 미분류 50백만) + 2팀 1건(80백만, 항상 제외)으로 합성 픽스처를 구성해 체크박스 라벨이 정확히 10.0/5.0/2.0/1.5억으로 표시되는지, 미분류 건이 기본 포함되어 1.5억에 반영되는지 확인.
- 100% 체크 해제 → "미입력 N건(XX억)" 금액이 정확히 10억만큼 줄어들고, "100% 구간은..." 각주가 숨겨지고, 범례/주석 문구에서 100% 관련 표현이 사라지는지 확인. 4개 모두 해제 → 메인 차트·보조 차트 둘 다 "포함할 구간을 하나 이상 선택해주세요"로 전환되는지 확인.
- 미분류 건 [상세보기] 클릭 시 프로젝트코드/프로젝트명/영업담당자/소속 원본값("CEO > 영업부문 > OKC영업본부")/금액이 정확히 표시되는지 확인. 체크 해제 시 50%(F) 구간 라벨 금액이 1.5억 → 1.0억으로 줄고(확도 필터와 무관하게 스코프 필터만 반영됨을 증명), 차트 아래 미분류 각주가 사라지는지 확인.
- 콘솔 로그로 "인벤토리 — 1팀 분류(후보) 건수", "소속 미분류 건수: 1 / 프로젝트코드: [OP-PF-UNASSIGNED]", "확도 구간별 금액(백만)" 등이 스펙대로 출력되는지 확인.
- 두 설정을 각각 다르게 바꾼 뒤(100% 해제 + 미분류 포함 해제) 페이지를 완전히 새로고침 — `pipeline_prob_filter`/`include_unassigned` idb 값과 실제 체크박스 상태가 독립적으로 정확히 복원되는지 확인. 본부 스코프 전환 시 미분류 체크박스가 숨겨지는 회귀 없음 확인. 콘솔 에러 없음.

---

### 2026-08-07 — 당월 예상액 차트 반영 및 현재 시점 표시 추가 (19차 작업)

#### 목표
회의자료의 "N월 매출 예상액" 값은 이미 `parseSalesExcel`이 파싱해 `series`에 포함시키고 있었지만, 차트에서는 확정 실적과 시각적으로 구분 없이 하나의 파란 실선에 섞여 있었다. 확정 실적 / 당월 예상 / 파이프라인 세 구간을 명확히 분리해서 보여주고, 오늘 날짜 위치도 표시.

#### 구현 내용
- **확정/예상 구분**: `findConfirmedAndForecastIdx(series)` 신규 — `series`에서 "확정(hasData && !isForecast) 마지막 인덱스"와 "예상(isForecast) 인덱스"를 찾음. 기존 `buildCumulativeActual`은 그대로 두고(확정+예상이 섞인 누적값을 계산하는 용도로 계속 사용), 렌더링 시점에 이 누적 배열을 세 조각으로 잘라서 표시: ① `confirmedArr`(확정월까지만, 나머지는 null) ② `forecastSegArr`(확정 마지막 지점 ↔ 예상월 지점 두 점만) ③ `pipelineArr`(예상월 지점부터 시작, 없으면 기존처럼 확정 마지막 지점부터).
- **1팀 재집계**: 기존 `computeTeam1ScopeData()`가 이미 "예상월과 동일한 billDate + 1팀 소속" 상세행을 재집계해 `team1Forecast`를 만들고 있었음(18차 작업 이전부터 존재) — 본부 요약행의 예상액을 그대로 쓰지 않고 소속 기준으로 다시 계산하는 요구사항은 별도 구현 없이 이미 충족되어 있었음을 픽스처로 확인.
- **차트 데이터셋**: "당월 예상"을 `borderColor: rgba(74,158,255,0.65)`(파란 계열, 반투명) · `borderDash:[4,3]` · `borderWidth:2` · `fill:false` · `spanGaps:true`로 추가. `pointRadius`를 12칸 배열로 만들어 예상월 인덱스만 5, 나머지는 0(확정 마지막 지점은 실적선 쪽 점(반지름 3)과 겹치므로 중복 표시하지 않음).
- **툴팁**: 공통 `tooltip.callbacks.label`에서 `item.dataset.label === '당월 예상'`일 때만 "— 확정 실적 아님 (예상)" 문구를 덧붙이도록 분기.
- **오늘 세로선**: 외부 annotation 플러그인 없이 `todayLinePlugin`(로컬 Chart.js 플러그인, `afterDraw`에서 캔버스에 직접 세로 점선 + 라벨을 그림)을 새로 작성해 `new Chart(ctx, { plugins: [todayLinePlugin], ... })`로 해당 차트에만 적용. `new Date().getFullYear() === 2026`일 때만 표시(차트 자체가 2026년 고정 데이터라 다른 연도에 표시하면 무의미). 라벨 공간 확보를 위해 `layout.padding.top: 18` 추가.
- **범례/상태줄/각주**: 범례에 "당월 예상 XX억 (점선)" 항목을 실적-파이프라인 사이에 삽입(예상월이 있을 때만). 차트 상단에 `#rev2-actual-status-line`("확정 실적: 2026년 N월까지 · M월은 예상액")을 실적 마지막 월/예상월을 자동 인식해 표시. 차트 아래 `#rev2-forecast-month-footnote`에 "※ N월은 확정 실적이 아닌 매출 예상액이며..." 각주 추가(예상월이 없으면 숨김).
- **요약 카드**: `computeSalesSummary`가 `confirmedEok`(확정만) · `forecastMonthEok`(예상월 단독 금액, 누적 아님) 필드를 추가로 반환. "누적 실적" 카드의 헤드라인 숫자 자체를 `confirmedEok`로 교체(기존엔 예상월 금액이 은근히 섞여 있었음)하고 아래에 "2026년 N월까지 확정" 서브텍스트 추가. "실적+파이프라인 합계" 카드 아래에 "확정 X + 예상 Y + 파이프라인 Z" 내역 추가(수치 자체는 `combinedEok = cumActualEok + pipeEok`로 기존과 동일하게 계산되므로 회귀 없음 — 표시만 세분화).

#### 검증
- 1~6월 확정 실적 + "7월 매출 예상액" 중복 2행(첫 번째만 사용되어야 함) + 1팀 상세행(1·3·5월 실적 + 7월 실적 1건)을 담은 합성 회의자료와, 1팀 소속 인벤토리 파일로 검증.
- 본부 스코프: 확정 실적 누적선이 정확히 [15.1, 33.2, 53.3, 76.5, 101.7, 129.9, null...]로 6월에서 끊기고, 당월 예상선이 [null×5, 129.9, 161.2, null...]로 6월→7월을 정확히 잇는지 `salesChart1.data.datasets`를 직접 조회해 확인. 중복된 두 번째 "7월 매출 예상액" 행(9999억)이 무시되고 첫 번째 값(31.3억)만 반영됐는지 확인.
- 1팀 스코프: 확정 실적이 5월까지(1팀은 6월 청구 없음)로 정확히 달라지고, 당월 예상이 본부 값(31.3억)이 아니라 1팀 7월 상세행 재집계값(3.0억)으로 정확히 계산되는지 확인 — 요약카드·범례·상태줄 전부 일관되게 3.0억 표시.
- REVENUE_FORECAST에 8월 입력값을 추가해 파이프라인선이 (기존처럼 마지막 확정월이 아니라) 당월 예상 지점(7월, 누적 10.0억)에서 정확히 이어져 시작하는지 `pipelineArr`를 직접 확인.
- 스크린샷으로 "당월 예상" 점선 구간, "오늘 (8월)" 회색 세로 점선(시스템 날짜 2026-08-07 기준 8월 위치), 범례 3항목, 상태줄, 하단 각주가 스펙과 정확히 일치하는 것을 시각적으로 확인.
- "누적 실적"/"실적+파이프라인 합계" 카드가 각각 "7.0억 · 2026년 5월까지 확정"과 "10.1억 · 확정 7.0 + 예상 3.0 + 파이프라인 0.1" 형식으로 정확히 표시되는지 확인. 콘솔 에러 없음.

---

### 2026-08-07 — 분기별 확정 매출실적 파일 업로드 기능 추가 (20차 작업)

#### 목표
1팀 누적 실적이 67.6억으로 나오던 문제(정확한 값은 100.6억) 수정. 원인은 주간 영업회의자료의 매출 상세 블록에 소속 컬럼이 없어 영업대표 이름 3명(조영오·양소예·정인수)으로만 필터링해왔기 때문 — 오수택·윤호영·담당자 미기재 건 약 33억이 구조적으로 누락되고 있었음. 소속 컬럼이 포함된 별도의 "분기별 확정 매출실적" 파일을 업로드받아 1팀 실적 산출의 우선 소스로 사용하도록 변경.

#### 구현 내용
- **새 파일 업로드 영역**: 기존 "주간 영업회의자료 업로드" 카드는 그대로 유지하고, 그 아래 "분기별 확정 매출실적 (선택)" 카드를 추가 — `multiple` 속성으로 여러 파일 동시 업로드 가능. 업로드된 파일은 칩 형태로 나열되고 각각 [×] 버튼으로 개별 삭제 가능.
- **파일별 독립 저장**: `CONFIRMED_REVENUE_FILES`를 파일명을 키로 하는 객체(`{ [fileName]: rows[] }`)로 설계 — 분기를 파일명이 아니라 각 행의 "매출실적일" 값에서 직접 산출하므로, 파일명이 스펙 예시("1팀_Q1_매출실적_확정.xlsx")와 다르거나 한 파일에 여러 분기가 섞여 있어도 정확히 동작. 같은 파일명으로 재업로드하면 해당 파일만 교체되고 다른 분기는 그대로 유지됨(요구사항 그대로).
- **`parseConfirmedRevenueSheet(wb, fileName)`**: 시트명에 "매출"과 "실적"이 모두 포함된 시트를 탐지, row[2]에서 프로젝트코드/프로젝트명/고객사명/소속/수주확도/매출순번/매출청구일/매출실적일을, row[3]에서 제품/상품/용역/유지보수/합계를 헤더 텍스트로 동적 탐지(고정 인덱스 없음, 합계만 못 찾으면 인덱스 13 폴백). 유효 행은 프로젝트코드가 'OP-'로 시작하는 행만. 금액은 백만원 단위 그대로 저장(원 단위 변환은 집계 시점에 수행).
- **소속 직접 판정**: 이 파일은 소속 컬럼을 자체적으로 가지고 있어 인벤토리 매핑이 전혀 필요 없음. 신규 `isRowInTeam1Scope(affiliationRaw)`(`classifyAffiliation` 재사용, 미분류는 기존 `INCLUDE_UNASSIGNED` 토글을 그대로 따름 — 인벤토리 파이프라인과 동일한 정책을 공유)로 판정. 월/분기 구분은 스펙 지시대로 "매출청구일"이 아니라 "매출실적일" 기준.
- **`aggregateConfirmedRevenueByMonth()`**: 모든 업로드 파일의 행을 합쳐 월별로 집계(1팀 판정된 행만, 금액은 ×1,000,000으로 원 단위 변환해 기존 파이프라인과 단위 통일). `buildConfirmedRevenueQuarterSummary()`는 별도로 분기별 건수/금액을 집계해 "확정 실적: Q1 61.5억(30건) · Q2 39.1억(41건) · 합계 100.6억" 형식의 요약 문구를 만듦.
- **데이터 소스 우선순위**: `computeTeam1ScopeData()`가 `hasConfirmedRevenue()`를 먼저 확인 — 확정 실적 파일이 하나라도 있으면 `aggregateConfirmedRevenueByMonth()`를 사용하고, 없으면 기존 방식(`aggregateTeam1ByMonth` — 주간 회의자료 상세행 + 인벤토리 소속 매핑 + 이름 폴백)으로 그대로 폴백. 당월 예상액의 1팀 재집계 로직도 동일한 분기 처리 — 확정 실적 파일이 있으면 그 파일에서 예상월에 해당하는 매출실적일 행을 재집계하고, 없으면 기존처럼 주간 회의자료 상세행을 사용. 영업본부 스코프는 이 변경과 완전히 무관(`getScopeData`가 본부일 때는 항상 `SALES_PARSED.hqMonths`만 사용).
- **폴백 안내**: 1팀 스코프인데 확정 실적 파일이 없으면 스코프 토글 아래 주황색 박스로 "확정 실적 파일이 없어 담당자명 기준으로 집계했습니다. 오수택·윤호영·담당자 미기재 건이 누락될 수 있습니다." 표시(`updateConfirmedRevenueFallbackNotice`, `computeTeam1ScopeData`가 반환하는 `usingConfirmedRevenue` 플래그로 판단).
- **저장**: idb 키 `confirmed_revenue`에 `CONFIRMED_REVENUE_FILES` 전체를 `roundNumbersDeep`으로 반올림해 저장. 페이지 로드 시 `restoreSalesTrendData()`보다 먼저 복원해야 첫 렌더링부터 정확한 소스가 반영되도록 초기화 순서 조정.
- **진단 로그**: 파일별 시트 목록, "매출 실적" 시트 탐지 결과와 컬럼 인덱스, 소속 값 고유 목록, 1팀 판정 건수와 제외 건수를 각각 `console.log`.

#### 검증
- 실제 컬럼 구조(0~13, 소속=4, 매출실적일=8, 합계는 row[3] 인덱스13)를 재현한 Q1·Q2 합성 파일(1팀 2건씩 + 2팀 1건씩 + 미분류 1건)로 검증 — 요약 바가 "확정 실적: Q1 17.0억(3건) · Q2 14.0억(2건) · 합계 31.0억"으로 정확히 표시되고, 2팀 건은 제외·미분류 건은 기본 포함되는지 확인. 확정 실적 업로드 전에는 주황 폴백 안내가 뜨고, 업로드 후 사라지는지 확인.
- 같은 파일명("1팀_Q1_매출실적_확정.xlsx")으로 값이 다른 파일을 재업로드 → 파일 개수는 그대로 2개 유지되면서 Q1 금액만 교체되는지 확인(17.0억→15.0억, Q2는 14.0억 그대로). [×] 버튼으로 개별 파일 삭제 시 해당 분기만 요약에서 빠지는지 확인.
- 콘솔 로그로 시트 목록·시트 탐지 결과·컬럼 인덱스·소속 고유 목록·"1팀 분류 3건/제외 1건"(누적 반영 시 "5건/2건")이 스펙대로 출력되는지 확인.
- 페이지를 완전히 새로고침한 뒤 업로드 파일 목록(칩)·요약 바·누적 실적 카드·폴백 안내 숨김 상태가 모두 그대로 복원되는지 확인.
- 영업본부 스코프로 전환 시 확정 실적 파일 유무와 무관하게 기존 수치(129.9억 확정 등)가 그대로 나오는지 확인(회귀 없음). 콘솔 에러 없음.

---

### 2026-08-10 — 상반기 확정 실적 파일 업로드 기능 추가 (팀실적 컬럼 기준) (21차 작업)

#### 목표
1팀 상반기 누적 실적이 67.6억으로 나오던 문제(정확한 값은 120.0억) 수정. 원인은 20차 작업까지도 매출 실적 산정이 결국 주간 영업회의자료의 매출 상세 블록(팀 구분 컬럼 없음)에 의존하고 있었기 때문 — 인벤토리 소속 매핑이나 이름 폴백으로는 매핑 실패·팀외 오분류가 누적되어 약 52억이 구조적으로 누락됨. "팀실적" 컬럼을 직접 포함한 별도의 "상반기 확정 실적" 파일을 업로드받아 1~6월 한정으로 최우선 데이터 소스로 사용하도록 함.

#### 구현 내용
- **새 업로드 영역**: 기존 "주간 영업회의자료"·"분기별 확정 매출실적" 업로드 카드는 그대로 두고, 그 아래 "상반기 확정 실적 (팀 분류 포함)" 카드를 신규 추가(단일 파일, 드래그&드롭+클릭).
- **`parseH1RevenueSheet(wb, fileName)`**: 시트명에 "매출"이 포함된 시트(없으면 첫 시트) 사용. 프로젝트코드/프로젝트명/고객사명/제품/상품/용역/유지관리/합계/매출실적영업대표/팀실적/매출실적일자 컬럼을 전부 row[2] 헤더 텍스트로 동적 탐지(고정 인덱스 사용 안 함, 프로젝트코드만 탐지 실패 시 기존 `scanOpCodeColumn` 폴백). 유효 행은 프로젝트코드가 'OP-'로 시작하는 행만.
- **팀 판정**: `isH1RowTeam1(teamValue)` — "팀실적" 컬럼 값을 공백 제거 후 정확히 "1팀실적"과 비교(부분일치 아님, 빈칸·"2팀 실적" 등은 전부 제외). "영업팀 확인"(발행예정/발행완료) 컬럼은 필터링에 전혀 사용하지 않음(스펙 요구사항).
- **날짜 파싱**: "매출실적일자"는 Date 객체와 문자열("2026-01-27") 두 형식이 섞여 있을 수 있어 기존 `parseExcelDateFull`(이미 두 형식 모두 처리하던 함수)을 재사용해 월 구분 기준으로 사용(매출청구일 아님). 파싱 실패 건은 집계에서 제외하고 콘솔에 카운트만 출력.
- **`aggregateH1RevenueByMonth()`**: 1팀 판정 통과 + 날짜 파싱 성공 행만 월별로 제품/상품/용역/유지보수/합계/건수 집계. `buildH1RevenueSummary()`가 분기별(Q1=1~3월/Q2=4~6월) 금액·전체 건수를 억원으로 환산.
- **데이터 소스 우선순위**: `computeTeam1ScopeData()`에서 기존 20차 작업 로직(분기 확정파일→주간회의자료 폴백)으로 `months`를 먼저 계산한 뒤, 상반기 확정 파일이 있으면 1~6월 인덱스만 `aggregateH1RevenueByMonth()` 결과로 덮어씀(7월 이후는 기존 로직 그대로 유지 — 부분 구간 override 방식). 반환값에 `usingH1Revenue` 플래그 추가.
- **검증 로그**: 업로드 시점에 한 번만(`processH1RevenueFile`에서 호출, 렌더링마다 반복 호출되지 않도록 주의) `verifyH1Revenue(months)`가 스펙 3번의 월별 기대값(1월 4건 2629.3백만 ~ 6월 17건 4613.0백만, 합계 71건 11995.4백만=120.0억)과 실제 파싱 결과를 월별로 비교해 "일치"/"불일치" 및 "전체 일치" 여부를 콘솔에 출력.
- **UI**: 요약 바에 "1팀 상반기 확정: Q1 60.7억 · Q2 59.2억 · 합계 120.0억 (71건)" 표시. 확정 파일이 없고 1팀 스코프일 때만 주황 안내 "상반기 확정 실적 파일이 없어 담당자명 기준으로 집계했습니다. 일부 건이 누락될 수 있습니다." 노출(`updateH1FallbackNotice`).
- **저장**: idb-keyval 키 `confirmed_h1_revenue`에 파일명+파싱된 행 배열 저장(`roundNumbersDeep` 적용), 새로고침 시 자동 복원. 같은 파일을 다시 올리면 전체 덮어쓰기(분기별 파일처럼 파일명별 부분 관리가 아니라 상반기 전체를 통째로 교체).
- 영업본부 스코프는 이번 변경과 완전히 무관 — `getScopeData`가 본부일 때는 여전히 `SALES_PARSED.hqMonths`만 사용.

#### 검증
- 스펙 3번의 월별 목표치를 정확히 재현하도록 만든 합성 픽스처(71건 유효 1팀 실적 행 + 2팀 1건/팀실적 공백 1건/날짜 파싱 실패 1건 제외 대상, 날짜는 Date객체·문자열 절반씩 혼합, 1/3은 "발행예정" 상태로 필터링되지 않음을 검증)를 업로드해 콘솔 검증 로그가 1~6월 전부 "일치", 합계 "전체 일치"(71건 11995.4백만=120.0억)로 출력되는지 확인.
- 요약 바 텍스트가 "1팀 상반기 확정: Q1 60.7억 · Q2 59.2억 · 합계 120.0억 (71건)"과 정확히 일치하는지 확인.
- 주간 영업회의자료(19차 작업 픽스처)를 함께 업로드해 "누적 실적" 요약카드가 정확히 "120.0억"(상반기 확정값 우선 반영)·"2026년 6월까지 확정"·"7월은 예상액" 상태줄까지 정확히 표시되는지 확인 — 19차 작업의 당월 예상 메커니즘과 충돌 없이 정상 연동.
- 페이지 완전 새로고침 후 업로드 파일명·요약 바·"누적 실적 120.0억" 카드가 IndexedDB로부터 그대로 복원되는지 확인.
- 상반기 확정 파일을 임시로 제거한 상태에서 주황 폴백 안내가 뜨고, 파일이 있으면 숨겨지는지 확인. 영업본부 스코프 전환 시 상반기 파일 유무와 무관하게 기존 수치(129.9억 확정 등)가 그대로 나오는지, 폴백 안내도 본부 스코프에서는 항상 숨겨지는지 확인(회귀 없음). 콘솔 에러 없음.

---

### 2026-08-10 — 매출 추이 탭 업로드 구조 정리 및 상반기/하반기 실적 분리 (22차 작업)

#### 목표
업로드 영역을 사용 빈도에 따라 정리(자주 쓰는 주간 영업회의자료는 상단, 거의 안 쓰는 상반기 확정 실적은 하단 접힘)하고, 20차 작업에서 추가했던 "분기별 확정 매출실적" 파일(소속 컬럼 기반)을 완전히 제거. 하반기(7~12월) 1팀 판별을 다시 이름 기준으로 단순화하고(주간 회의자료에는 팀 구분 컬럼이 없어 인벤토리 소속 매핑에 의존해왔던 방식을 걷어냄), 오수택을 선택적으로 포함할 수 있는 체크박스를 추가.

#### 구현 내용
- **업로드 영역 2개로 정리**: 20차 작업의 "분기별 확정 매출실적" 업로드 카드(HTML/CSS/JS 전체 — `parseConfirmedRevenueSheet`/`CONFIRMED_REVENUE_FILES`/`aggregateConfirmedRevenueByMonth`/`buildConfirmedRevenueQuarterSummary`/`isRowInTeam1Scope`/`renderConfirmedRevenueUI`/`save·restoreConfirmedRevenue`/`setupConfirmedRevenueUpload`/`updateConfirmedRevenueFallbackNotice`)를 완전히 삭제. 더 이상 쓰이지 않게 된 `buildInventoryAffiliationMap`도 함께 삭제(인벤토리 소속-매핑 방식 자체를 하반기 판정에서 걷어냈으므로).
- **화면 최종 순서**: 본부/1팀 토글 → 오수택 포함 체크박스(1팀 선택 시만) → (기존 소속 미분류/폴백 안내) → 주간 영업회의자료 업로드 → 파이프라인 포함 구간 체크박스 → 요약 카드 → 메인 차트 → 보조 차트 → 매출 예상 입력(접힘) → 상반기 확정 실적(접힘). 기존 `#rev2-prob-filter-card`를 `#rev2-content` 내부 첫 자식으로 이동(기존엔 요약 카드 뒤에 있었음).
- **상반기 확정 실적 업로드 재배치**: 화면 맨 아래로 이동, `매출 예상 입력` 카드와 동일한 접기/펼치기 패턴(`toggleH1Section`, 기본 접힘) 적용. 섹션 제목 옆에 상태 배지(`#rev2-h1-status-badge`) — 등록 시 초록색 "✓ 등록됨 · 120.0억(71건) · 2026-08-10 등록", 미등록 시 주황색 "미등록 — 상반기 실적이 정확하지 않을 수 있습니다". 등록 시각(`CONFIRMED_H1_REVENUE_REGISTERED_AT`)을 새로 추가해 idb에 함께 저장, `formatDateYMD()`로 로컬 날짜(YYYY-MM-DD)만 표시. `[삭제]` 버튼(`removeH1RevenueFile`, confirm 후 삭제) 추가 — 파일 자체를 지우면 상반기도 하반기와 동일하게 담당자명 기준으로 폴백됨. 상단 폴백 안내 문구도 "상반기 확정 실적 파일을 업로드해주세요.<br>현재는 담당자명 기준으로 집계됩니다."로 교체(기존 "…집계했습니다. 일부 건이 누락될 수 있습니다."에서 변경).
- **하반기(7~12월) 1팀 판별 — 이름 기준으로 단순화**: `aggregateTeam1ByMonth`가 더 이상 `buildInventoryAffiliationMap`으로 인벤토리 소속을 조회하지 않고, 신규 `isTeam1H2ByName(rep)`(기본 3명 조영오·양소예·정인수 + 체크박스로 선택하는 오수택)로 직접 판정. `computeTeam1ScopeData`도 "확정 실적 파일 유무" 분기를 제거하고 [하반기: 이름 기준 집계] → [상반기 1~6월: 상반기 확정 실적 파일이 있으면 덮어쓰기] 두 단계로 단순화. 당월 예상액 재집계도 동일하게 `isTeam1H2ByName` 사용.
- **오수택 포함 체크박스**: `INCLUDE_OSTAEK`(기본 true), 스코프 토글 바로 아래 1팀 선택 시에만 노출. 라벨에 "오수택 포함 (5.6억)" 형식으로 하반기(7월 이후) 기여 금액을 병기(`getOstaekH2AmountEok` — billDate 월이 7 이상인 그의 상세행만 합산). "※ 체크박스는 하반기(7~12월) 실적에만 적용됩니다." 안내 문구 포함. idb 키 `include_ostaek`로 저장/복원, `clearAllStoredData`에서 함께 초기화.
- **상반기/하반기 요약 바**: `#rev2-h1h2-summary-bar` 신규 — "상반기 확정 120.0억(71건) · 하반기 10.1억 · 합계 130.1억" 형식(1팀 스코프에서만, 요약 카드 바로 위). `sd.months[1..6]`/`sd.months[7..12]`를 합산해 계산(두 집계 함수 모두 월별 `count` 필드를 새로 추가해 상반기 건수 표기에 사용).
- **차트 상반기/하반기 구분선**: 신규 `h1h2DividerPlugin`(로컬 Chart.js 플러그인) — 6월과 7월 사이에 얇은 회색 점선 세로줄을 그림, 1팀 스코프일 때만(`h1h2Divider: { show: scope === '팀1' }`) 활성화. 기존 `todayLinePlugin`과 함께 `plugins:[todayLinePlugin, h1h2DividerPlugin]`로 등록.
- 기존 "소속 미분류 건 포함" 체크박스(18차 작업)와 "소속 정보를 찾지 못해…" 안내는 인벤토리 기반 파이프라인 후보(`getPipelineCandidateRows`) 전용 로직이라 이번 변경과 무관하게 그대로 유지(매출 실적 산정과는 별개의 데이터 소스).

#### 검증
- 주간 영업회의자료 합성 픽스처(하반기 7~12월에 조영오·양소예·정인수·오수택 각 1건 + 팀외 인원 1건 + 상반기 1~6월에 조영오·오수택 각 1건 fallback 테스트용)와 상반기 확정 실적 픽스처(21차 작업의 71건 검증 픽스처 재사용)를 실제로 업로드해 확인.
- `getScopeData('팀1')`로 월별 집계를 직접 조회 — 하반기 7~11월이 정확히 이름 기준으로 집계되고 팀외 인원(박다른팀, 12월)이 제외되는지, 오수택 라벨 금액이 "(5.6억)"(10월 3.0억+11월 2.6억)으로 정확한지 확인.
- 오수택 체크 해제 → 10·11월 데이터가 통째로 사라지고(두 달 모두 오수택 전용 데이터였음) 하반기 요약이 10.1억→4.5억으로, 당월 예상(11월 forecast) 재집계가 260,000,000→0으로 정확히 바뀌는지, 체크 재활성화 시 원래 값으로 복원되는지 확인.
- 상반기 확정 실적 파일을 `[삭제]`로 제거 → 상단 폴백 안내가 뜨고, 1~6월이 이름 기준 폴백값(2월 오수택 0.4억, 3월 조영오 0.5억)으로 정확히 전환되는지 확인. 재업로드 시 상태 배지가 "✓ 등록됨 · 120.0억(71건) · 2026-08-10 등록"으로(오늘 날짜 정확히 반영) 갱신되는지 확인.
- 영업본부 스코프 전환 시 오수택 체크박스·상반기/하반기 요약 바·차트 구분선이 모두 숨겨지고 기존 수치(확정 63.9억 등)가 그대로 나오는지 확인(회귀 없음).
- 페이지 완전 새로고침 후 스코프·오수택 체크 상태·상반기 확정 실적(파일명·배지·등록일)·주간 회의자료 업로드 상태가 전부 IndexedDB로부터 정확히 복원되는지 확인.
- `clearAllStoredData()` 실행 후 오수택 체크박스가 기본값(true)으로, 상반기 확정 실적이 미등록 상태로, 관련 idb 키(`include_ostaek`/`confirmed_h1_revenue`)가 모두 삭제되는지 확인.
- `.trend-layout`/`#rev2-content`의 자식 DOM 순서를 직접 조회해 스펙이 요구한 9단계 최종 순서와 정확히 일치하는지 확인. 전 과정에서 콘솔 에러 없음.

---

### 2026-08-10 — 차트 월 클릭 시 사업 목록 팝업 추가 (23차 작업)

#### 목표
매출 추이 탭 메인 차트에서 파이프라인 전망(초록 점선) 구간의 월을 클릭하면, 그 달에 매출 예상으로 잡힌 사업 목록을 팝업으로 보여주는 기능 추가. 실적 구간(파란 실선)의 월은 클릭해도 아무 반응이 없어야 함.

#### 구현 내용
- **클릭 가능 월 판정**: `isPipelineClickableMonth(idx, pipelineStartIdx, pipelineArr)` — `pipelineArr[idx]`가 존재하고 `idx > pipelineStartIdx`일 때만 true. `pipelineStartIdx`(당월 예상 또는 마지막 확정월)와 겹치는 시작점은 실적/당월예상 쪽 데이터라 제외, 그 이후 순수 전망 구간만 클릭 대상으로 인정.
- **목록 구성**: `buildMonthForecastList(month, forecastData)` — `forecastData.candidates`(확도 구간 체크박스·본부/1팀 스코프가 이미 반영된 파이프라인 후보 목록)를 순회하며, 각 사업의 `REVENUE_FORECAST[code]` 항목 중 `월`이 대상 월과 일치하는 항목들의 금액을 합산해 사업당 한 줄로 집계(같은 사업이 여러 항목으로 나뉘어 같은 달을 가리키는 경우 자동 합산, 다른 달을 가리키는 항목은 각자 해당 월 팝업에서만 표시). 금액 큰 순 정렬.
- **클릭 처리**: `handleChartMonthClick(idx, pipelineStartIdx, pipelineArr, forecastData)` — 클릭 가능 월이 아니면 무시, 목록이 비어 있으면(해당 월에 매출 예상 데이터 없음) 팝업을 띄우지 않음.
- **Chart.js 연동**: `renderSalesChart1`의 `options`에 `onClick`(`getElementsAtEventForMode(evt,'index',{intersect:false},true)`로 인덱스 판별 → `handleChartMonthClick` 호출)과 `onHover`(같은 방식으로 인덱스 판별 → `isPipelineClickableMonth` 결과에 따라 `chart.canvas.style.cursor`를 `pointer`/`default`로 전환) 추가.
- **팝업 UI**: `#rev2-month-popup-overlay`(반투명 검정 `rgba(0,0,0,0.35)` 오버레이, `position:fixed`, flex 중앙 정렬) + `.rev2-month-popup`(최대 900px, 다크테마 카드). 헤더는 "2026년 N월"(16px 굵게) + "N건 · X.X억"(13px 회색) + × 닫기 버튼을 한 줄에 배치(`white-space:nowrap`). 목록 각 줄은 확도 배지(44px 고정폭, `PIPELINE_COLORS`/`PIPELINE_LABELS` 재사용해 100%=파랑/90%=초록/75%=노랑/50%(F)=주황) + 사업명(담당자를 괄호로 붙여 표기, 담당자는 회색) + 우측 정렬 금액("2,100.0백만원", 기존 `fmtAmt` 재사용) 3요소를 가로 배치, `white-space:nowrap` + `text-overflow:ellipsis`로 말줄임 처리(사업명에 `title` 속성으로 전체 텍스트 툴팁). 목록 아래 구분선 + "합계 … XXX.X백만원" 행.
- **닫기**: × 버튼(`closeMonthForecastPopup`), 오버레이 배경 클릭(`onclick="if(event.target===this) closeMonthForecastPopup()"` — 팝업 내부 클릭은 통과), `document`에 등록한 전역 `keydown` 리스너로 Escape 키 처리(팝업이 열려 있을 때만 반응).
- 팝업을 열고 닫아도 차트·요약카드 등 다른 화면 상태는 전혀 변경하지 않음(단순 오버레이 표시/숨김).

#### 검증
- 인벤토리 파이프라인 후보 6건(100%/90%/75%/50%(F) 구간, 그중 소속 미분류 1건·2팀 1건 포함)과 주간 영업회의자료(1~6월 확정 + 7월 당월예상)를 실제로 업로드하고, `REVENUE_FORECAST`에 8월(3건, 그중 한 사업은 항목 2개로 분할)·9월(2건, 그중 한 사업은 10월에도 별도 항목)·10월(1건)·11월(1건)·7월(1건, 실적 구간이라 클릭 차단 검증용) 매출 예상을 직접 입력.
- 실제 Chart.js `onClick`/`onHover`가 차트 인스턴스에 함수로 등록됐는지 확인한 뒤, `handleChartMonthClick`을 렌더 함수와 동일한 방식으로 재계산한 `pipelineStartIdx`/`pipelineArr`로 직접 호출해 인덱스별 결과를 확인 — 3월(확정 구간)·7월(당월예상 시작점) 클릭 시 팝업이 뜨지 않음, 8월 클릭 시 3건(90%·100%·50%(F) 미분류, 500/400/25백만) 금액 내림차순 정렬 + 합계 925.0백만원("3건 · 9.3억"), 9월 클릭 시 2건(200/50백만, 합계 250.0백만원 — 같은 사업의 10월분과 섞이지 않음), 10월 클릭 시 해당 사업의 10월분만(50.0백만원), 11월 클릭 시 2팀 사업 포함(80.0백만원, 본부 스코프이므로 정상 포함), 12월(데이터 없음) 클릭 시 팝업이 뜨지 않음을 모두 확인 — 각 월의 합계가 `computeForecastSummary('본부').monthlyMillion`과 정확히 일치.
- ESC 키·오버레이 배경 클릭 시 닫히고, 팝업 내부(목록 영역) 클릭 시 닫히지 않는지 확인. × 버튼 클릭으로도 정상적으로 닫히는지 확인.
- 90% 확도 구간 체크박스를 해제한 뒤 같은 8월 팝업을 다시 열어 90% 사업이 목록에서 빠지고 합계가 925.0→425.0백만원("2건 · 4.3억")으로 정확히 줄어드는지 확인(확도 필터 반영), 체크박스를 다시 켜서 원상 복구.
- 전 과정에서 콘솔 에러 없음. `clearAllStoredData()`로 테스트 데이터 정리 후 로컬 서버 종료.

---

### 2026-08-11 — 엑셀 다운로드 컬럼 구조 복원 및 헤더명 변경 (24차 작업)

#### 목표
인벤토리 비교 탭 엑셀 다운로드가 11차 작업에서 화면 검색 기능과 함께 "원본 비고"(영업담당자가 엑셀에 직접 작성한 비고) 컬럼을 엑셀에도 추가하면서 14개 컬럼(구분·고객사명·프로젝트코드·프로젝트명·영업담당자·수주확도·수주상태·변동내역·비고·제품·상품·용역·유지보수·총합계)이 되어 있던 것을, 시스템 자동 생성 "변동내역" 컬럼만 남기고 원본 비고 컬럼은 제거해 13개 컬럼으로 복원. 원본 비고는 화면 검색 결과 카드에서만 확인하도록 함(화면 표·검색 기능 자체는 변경 없음).

#### 구현 내용
- `downloadInvExcel()`의 `ws.columns` 너비 배열에서 비고(원본) 컬럼의 `{width:30}` 항목 제거(14개 → 13개).
- 헤더 배열에서 `'비고'` 제거 — 최종 헤더: `구분/고객사명/프로젝트코드/프로젝트명/영업담당자/수주확도/수주상태/변동내역/제품(백만)/상품(백만)/용역(백만)/유지보수(백만)/총합계(백만)`.
- 데이터 행: `it.note`(원본 비고)를 쓰던 `noteCell` 코드 블록 삭제, 제품/상품/용역/유지보수 컬럼을 9~12번(기존 10~13번)으로, 총합계를 13번(기존 14번)으로 한 칸씩 앞당김.
- 요약 행 3개(1행 전주대비 요약, 2행 항목별 변동폭, 구간이동 내역 각 줄)의 병합 범위를 `A:N`(14열)에서 `A:M`(13열)으로 축소. "금주 합계" 행의 라벨 병합도 `A:I`에서 `A:H`로 축소하고, 카테고리 합계 셀 위치를 9~13번으로 이동.
- **항목별 변동폭 행 구분자 수정**: 기존에 3칸 공백(`'   '`)으로 항목을 구분하던 것을 스펙에 명시된 `' / '`로 변경(`제품 ▼93.1백만 / 상품 ─ / 용역 ▲53.4백만 / 유지보수 ─ / 총합계 ▼39.7백만` 형식). 색상 규칙(증가=초록 굵게, 감소=빨강 굵게, 무변동=회색 "─")은 기존 그대로 유지.
- 화면 표(14열, 비고 포함)·검색 결과 카드·`buildCatDiffData`·화면용 항목별 변동폭 표시(공백 구분)는 이번 변경과 무관하게 그대로 유지 — 엑셀 다운로드 함수(`downloadInvExcel`)만 수정.

#### 검증
- 신규 1건·변동 1건(제품 감소 포함, 원본 비고에 개행 포함된 텍스트)·삭제 1건이 섞인 합성 픽스처를 90% 구간에 업로드해 비교 실행 후, `downloadInvExcel()`을 직접 호출하면서 `URL.createObjectURL`/`a.click`을 가로채 실제 다운로드 없이 생성된 엑셀 바이너리를 캡처.
- 캡처한 바이너리를 SheetJS로 다시 읽어 헤더 행이 정확히 13개 컬럼("비고" 없음)인지, 각 데이터 행 값이 13개 컬럼에 정확히 채워지는지, 어디에도 원본 비고 텍스트("고객사 요청으로 일정 연기…")가 남아있지 않은지 확인.
- `sheet['!merges']`로 병합 범위를 직접 조회해 요약/항목변동폭/구간이동 줄이 정확히 `A:M`(0~12열)으로, "금주 합계" 라벨이 `A:H`(0~7열)로 병합됐는지 확인.
- 2행 항목별 변동폭 텍스트가 `제품 ▼143.1백만 / 상품 ▲250.0백만 / 용역 ▲203.4백만 / 유지보수 ─ / 총합계 ▲310.3백만` 형식(`/` 구분자)으로 정확히 출력되는지 확인.
- 같은 바이너리를 ExcelJS로 다시 읽어 신규(초록 배경)/삭제(주황 배경+취소선)/제품감소(빨강 배경, 최우선순위) 행 색상과 변동된 숫자 셀(제품·총합계)의 빨간 볼드 폰트가 새 컬럼 위치(9번·13번)에서도 정확히 적용되는지 확인. 콘솔 에러 없음.

---

### 2026-08-11 — 체크박스 라벨 금액 기준 통일 (25차 작업)

#### 목표
매출 추이 탭의 "파이프라인 포함 구간" 체크박스 라벨이 후보 사업의 원본 수주금액 합계("전체")를 표시하는 반면, 요약 카드·보조 차트·월별 팝업은 실제 입력된 매출 예상 중 2026년분("2026년")만 표시하고 있어 같은 화면 안에서 기준이 서로 달라 합산이 안 맞아 보이는 문제 수정. 계산 자체는 둘 다 정확했음 — 표시 기준만 통일.

#### 구현 내용
- **`computeForecastSummary(scope)` 확장**: 기존에는 체크박스 라벨용으로 `sectionAmounts`(구간별 원본 수주금액 합계, 연도 무관, PIPELINE_PROB_FILTER와 무관하게 전체 후보 기준)만 계산했음. 여기에 같은 방식(전체 후보 기준, 자기 자신의 체크 상태와 무관)으로 `sectionAmounts2026`(입력된 매출예상 중 2026년분만)과 `sectionAmountsExcluded`(2027년 이후분)를 추가로 계산 — 후보 전체(`cand.rows`)를 순회하며 각 후보의 `REVENUE_FORECAST` 입력을 연도별로 분리 집계. 기존에 체크된 구간만 집계하던 `bySectionMillion`/`monthlyMillion`/`inputTotalMillion`(차트·요약카드에 실제 반영되는 값)은 그대로 유지.
- **체크박스 라벨 기준 변경**: `renderProbFilterUI(forecastData)`가 이제 `sectionAmounts` 대신 `sectionAmounts2026`을 라벨 금액으로 사용 — 요약카드·보조차트·월별 팝업과 완전히 동일한 기준이 됨. 100% 구간도 예외 없이 동일 로직 적용(요청사항의 "100% 잔여 라벨도 동일 기준" 항목은 별도 분기 없이 공용 로직으로 자동 충족).
- **툴팁 추가**: 각 체크박스에 `title` 속성으로 "2026년 X억 / 전체 Y억 (2027년 이후 Z억 제외)" 형식의 상세 내역 표시.
- **자기 자신 체크 상태와 무관하게 라벨 유지**: `sectionAmounts`/`sectionAmounts2026`/`sectionAmountsExcluded` 모두 `PIPELINE_PROB_FILTER` 필터링 이전의 전체 후보(`cand.rows`)에서 계산하므로, 체크 해제해도 그 구간 자신의 라벨 금액은 그대로 유지됨(기존 18차 작업의 "체크박스 라벨은 필터와 무관하게 항상 전체 금액을 보여준다" 설계 원칙을 "전체" 대신 "2026년" 기준으로 그대로 계승).
- **검증 표시 추가**: 보조 차트 아래 `#rev2-validation-line`(회색 11px) — "검증: 구간 합계 = 파이프라인 카드 ✓ / 월별 합계 = 파이프라인 카드 ✓". `sectionSum2026`(체크된 구간의 `bySectionMillion` 합)과 `monthlySum2026`(체크된 구간의 `monthlyMillion` 합)을 각각 `inputTotalMillion`(파이프라인 카드 금액)과 0.05백만 오차 이내로 비교, 불일치 시 빨간색으로 전환하고 차이 금액(억원)을 함께 표기.
- **검증 로그**: `computeForecastSummary` 실행 시마다 구간별 `{구간, 건수, 전체, 2026년, 2027년이후}` 배열과, 구간 합계·월별 합계 각각을 파이프라인 카드 금액과 비교한 일치/불일치 로그를 `console.log`로 출력(불일치 시 차이 금액 포함).

#### 검증
- 인벤토리 파이프라인 후보 6건(100%/90%/75%/50%(F) 구간, 50%(F)에 3건)을 업로드하고, 일부 후보에 2026년과 2027년 매출예상을 나누어 입력한 합성 픽스처로 검증(예: 90% 구간 500백만 중 300백만은 2026년, 200백만은 2027년) — `computeForecastSummary('본부')`가 반환한 `sectionAmounts`(전체 500)/`sectionAmounts2026`(300)/`sectionAmountsExcluded`(200)가 정확히 일치.
- 화면에서 체크박스 라벨이 "90% (3.0억)"(2026년 기준)로 표시되고, 툴팁이 "2026년 3.0억 / 전체 5.0억 (2027년 이후 2.0억 제외)"으로 정확히 표시되는지 확인. 보조 차트(확도별 파이프라인 구성) 범례가 체크박스 라벨과 정확히 같은 숫자(90% 3.0억 등)를 보여줘 두 UI 간 기준이 완전히 일치함을 직접 확인.
- 90% 체크박스를 해제해도 자기 자신의 라벨 금액(3.0억)이 그대로 유지되는지 확인(기존 설계 원칙 유지 회귀 없음).
- 검증 표시가 정상 케이스에서 "✓ / ✓"로 녹색 없이 회색으로 표시되고, `renderValidationLine`에 인위적으로 불일치 데이터를 주입했을 때 "구간 합계 ≠ 파이프라인 카드 (차이 1.1억)" 형태로 빨간색 전환 및 정확한 차이 금액이 표시되는지 확인.
- 콘솔 로그에서 구간별 `{구간, 건수, 전체, 2026년, 2027년이후}` 4개 행과 "4구간 2026년 합계... → 일치", "월별 합계... → 일치" 로그가 스펙대로 출력되는지 확인. 전 과정에서 콘솔 에러 없음.

---

### 2026-08-11 — 요약 카드 재구성, 연간 기준 통일, 미매칭 사업 선택 기능 추가 (26차 작업)

#### 목표
매출 추이 탭의 요약 카드·체크박스·용어를 전면 개편. 기존 6개 카드(누적 실적/파이프라인/실적+파이프라인 합계/부족액/누적 달성률)를 "상반기 확정 + 하반기 매출예상"을 명확히 분리해 보여주는 새 6개 카드로 교체하고, 달성률·증감 계산을 상반기 기준과 연간 기준이 혼용되던 것을 연간 기준으로 통일. "파이프라인" 용어를 전면 "매출예상"으로 교체. 100% 구간 중 매출 실적과 매칭되지 않은 사업을 사용자가 건별로 포함/제외 선택할 수 있는 기능 추가.

#### 1. 확도 체크박스 영역
- 제목을 "파이프라인 포함 구간" → "매출예상 반영사업"으로 변경.
- `.rev2-prob-filter-row`의 `gap`을 24px → 32px로 확대(항목 사이 간격), `row-gap:16px` 추가(줄바꿈 시 세로 간격). 체크박스↔라벨·라벨↔금액 간격(6px)과 상하 padding(기존 `.chart-card`의 24px/16px)은 이미 요구사항을 충족하고 있어 변경 없음. `flex-wrap:wrap`도 기존에 이미 적용되어 있었음.
- 체크박스 라벨 금액 기준을 "2026년" 값으로 통일하는 작업은 직전(25차) 작업에서 이미 완료된 상태였음 — 이번 작업에서는 그 기준을 그대로 유지.

#### 2~3. 요약 카드 재구성
- `computeSummaryCardsV2(scope, forecastData)` 신규 — ①연간 목표(`annualTargetTotal`) ②26년 총 누적예상 실적(=③+④) ③상반기 누적실적(`getScopeData(scope).months[1..6]` 합계, 원 단위→억 변환) ④하반기 예상실적(`forecastData.h2TotalMillion`, 확도 체크박스 반영) ⑤목표 대비 증감(②-①) ⑥예상 달성률(②÷①×100)을 계산. 기존 `computeSalesSummary`(상반기 기준 `cumRate` 등 옛 로직)는 삭제하지 않고 그대로 유지 — `saveSalesTrendData()`의 미사용 진단 스냅샷에서만 계속 참조되므로 건드리지 않음(화면에는 더 이상 렌더링되지 않음).
- **연간 기준 통일(핵심 수정)**: 기존 "누적 달성률" 카드는 `cumActualEok ÷ 그 시점까지의 누적 목표 × 100`(상반기~해당월까지의 부분 목표 기준)이었던 반면, 새 "⑥ 예상 달성률"은 `totalExpectedEok ÷ annualTargetEok × 100`(항상 연간 목표 전체 기준)으로 계산 — 합성 픽스처로 820.68억 목표에 47.5억 실적일 때 정확히 5.8%(=47.5/820.68×100)가 나오는지 확인해 상반기 기준 혼용 버그가 없는지 검증.
- **카드 스타일**: `.summary-card-highlight`(② 전용, accent색 테두리+box-shadow, `.s-value-xl`로 폰트 30px) / `.summary-card-muted`(③④ 전용, 배경 `#191c29`, 라벨·값·서브텍스트 모두 `--text-sub` 색, 값 폰트 20px로 한 단계 축소) CSS 신규 추가.
- 제거된 카드: "파이프라인(입력 기준)", "실적+파이프라인 합계", "누적 달성률"(상반기 기준) — `renderSalesSummary` 함수 자체를 새 6카드 HTML로 전면 교체.

#### 4. 용어 변경
- 체크박스 제목(위 1번), 메인 차트 제목("목표 대비 누적 실적 + 파이프라인 전망" → "연간 목표 대비 실적 및 매출예상 추이"), 차트 범례("파이프라인 X억" → "실적 예상 X억", Chart.js 데이터셋 `label`도 "파이프라인 반영 전망" → "실적 예상"으로 동일하게 변경해 툴팁 텍스트도 통일), 보조 차트 제목("확도별 파이프라인 구성" → "수주확도별 매출예상 구성")을 변경. 추가로 인벤토리 미분석/매출예상 미입력 시 표시되는 안내 문구 4곳("...파이프라인 전망이 함께 표시됩니다" 등)도 "매출예상"으로 함께 통일(요청에 명시되진 않았으나 같은 용어 정리 취지).

#### 5. 주석 문구 전면 교체
- 기존 `buildPipelineSourceFootnote()`(체크된 구간 구성에 따라 동적으로 문구를 조립하던 함수)를 완전히 삭제하고, 정적 2줄 텍스트를 HTML에 직접 하드코딩(더 이상 구간 구성에 따라 문구가 달라지지 않음 — 스펙 문구 자체가 고정 문구이므로).
- "100% 구간..." 주석도 정적 텍스트로 교체(기존엔 고정 문구였으므로 내용 변경만, 표시/숨김 로직인 `PIPELINE_PROB_FILTER['100']` 조건부는 그대로 유지).
- "2027년 이후 제외" 주석: 문구를 "...제외됩니다" → "...제외했습니다"로 변경, 기존엔 0건일 때 "※ 매출청구일이 2027년 이후인 사업은 올해 전망에서 제외됩니다"라는 대체 문구를 보여줬으나 이번에 0건이면 완전히 숨기도록 변경(`style.display='none'`).
- HTML상 DOM 순서를 매출예상 설명 → 100%잔여 설명 → 미매칭 100% 안내(신규 패널 포함) → 2027년 이후 제외 → 목표 분기 배분 순으로 재배치(기존엔 매출예상 설명 → 2027제외 → 100%잔여 → 미매칭 순으로 스펙과 어긋나 있었음).
- 목표 분기 배분 주석은 "수립되어" 뒤에 쉼표 추가, 2줄로 줄바꿈(`<br>`) — 그 외 문구·상시표시 동작은 기존과 동일.

#### 6. 미매칭 100% 사업 개별 선택 기능
- `getPipelineCandidateRows`의 100% 구간 매칭 실패 분기를 수정 — `UNMATCHED_100_EXCLUDED`(Set, 사용자가 명시적으로 제외한 사업코드) 확인 후 제외되지 않은 건만 잔여분 전액을 후보에 반영(`remainder = included ? order : 0`). `unmatched` 배열에 `sales`(영업담당자)·`amountMillion`·`included` 필드를 추가해 UI에서 바로 사용할 수 있게 함.
- **신규 감지**: `syncUnmatched100Seen()` — `runInvCompare()` 완료 시점마다 호출되어(항상 '본부' 스코프로 조회, unmatched 목록 자체가 스코프 무관이므로) 이번 분석의 미매칭 코드와 `UNMATCHED_100_SEEN`(지금까지 한 번이라도 미매칭이었던 코드, 계속 누적)을 비교해 `UNMATCHED_100_NEW_CODES`(신규 코드 배열, 다음 분석 전까지 유지)를 계산한 뒤 SEEN을 갱신·저장. 실제 UI 렌더링(`renderSalesTrend` 등 매 렌더링)에서는 이 SEEN 갱신을 반복 호출하지 않고 `runInvCompare()` 완료 시점에만 1회 갱신 — 그래야 "신규" 배너가 체크박스 조작 등 일반 렌더링마다 사라지지 않고 다음 실제 재분석 전까지 안정적으로 유지됨.
- **UI**: 기존 `.rev2-unmatched-notice`(단순 목록)를 완전히 걷어내고 `.rev2-unmatched-panel`(체크박스 열 포함 표, [전체 선택]/[전체 해제] 버튼, "N건 중 M건 포함 · XX억" 요약 바, 신규 안내 배너)로 교체. 체크박스 변경(`toggleUnmatched100`) 시 `UNMATCHED_100_EXCLUDED`를 갱신하고 `renderSalesTrend()` 전체 재실행 — ②④⑤⑥·체크박스 라벨(100% 잔여)·메인 차트·보조 차트·월별 팝업이 전부 `getPipelineCandidateRows`를 다시 타면서 자동으로 재계산됨(별도 배선 불필요).
- **저장**: idb 키 `unmatched_100_excluded`(제외 코드 배열), `unmatched_100_seen`(누적 관측 코드 배열) — 인벤토리에서 사라진 코드도 삭제하지 않고 그대로 보관(Set 구조상 자연히 유지됨, 별도 정리 로직 없음).
- **초기화 순서 수정**: `restoreInvFiles()`가 저장된 인벤토리 데이터를 찾으면 `DOMContentLoaded` 시점에 즉시 동기적으로 `runInvCompare()`를 실행하므로, 그 안에서 호출되는 `syncUnmatched100Seen()`이 올바른 이전 선택값을 보고 판단하도록 `restoreUnmatched100Excluded()`/`restoreUnmatched100Seen()` awiat를 `restoreInvFiles()` 호출보다 먼저로 순서를 옮김.

#### 7~8. 체크박스 연동, 자체 검증
- `computeForecastSummary`에 `bySectionMillionH2`(구간별 7~12월 2026년 입력분, 체크된 구간만)·`h2TotalMillion`(하반기 예상실적 총액, 카드④ 기준값)·`h2InputCount`(하반기 입력 있는 사업 수) 신규 추가. 기존 `bySectionMillion`(1~12월 전체)·`sectionAmounts`류(체크박스 라벨/툴팁용, 25차 작업에서 이미 구현)는 그대로 유지.
- 검증 라인 문구를 "구간 합계 = 파이프라인 카드" → "확도 합계 = 하반기 예상"으로 변경, 비교 기준값도 기존 `inputTotalMillion`(전체 12개월) 대신 `h2TotalMillion`(7~12월만)으로 교체 — `sectionSumH2`(구간별 H2 누계)·`monthlySumH2`(월별 7~12월 누계) 두 독립 누산값을 `h2TotalMillion`과 각각 0.05백만 오차 이내로 비교.

#### 검증
- 100% 구간 3건(A: 매칭 성공, 잔여 300백만 / B·C: 매칭 실패, 각 500·300백만) + 90%/75%/50%(F) 각 1건, 주간 영업회의자료(1~6월 확정 각 5억)로 합성 픽스처 구성. `REVENUE_FORECAST`에 A~F 각각 하반기 월과 C는 2027년 분할 항목까지 입력.
- 카드 6개 값을 직접 조회해 손계산과 전부 일치 확인: ①820.7억 ②47.5억(하이라이트 클래스) ③30.0억("2026년 6월까지 확정", 연한 클래스) ④17.5억("입력 6건", 연한 클래스) ⑤-773.2억(빨강) ⑥5.8%(빨강) — ⑤⑥ 모두 연간 목표(820.68억) 기준으로 정확히 계산됨을 확인(상반기 목표로 나뉘는 회귀 없음).
- 체크박스 라벨·툴팁이 25차 작업 그대로 정상 동작(100% 잔여 "2026년 10.0억 / 전체 11.0억 (2027년 이후 1.0억 제외)" 등), 검증 라인이 "확도 합계 = 하반기 예상 ✓ / 월별 합계 = 하반기 예상 ✓"로 정확히 표시되는지 확인.
- 미매칭 패널 [상세보기] 클릭 시 "2건 중 2건 포함 · 8.0억" 요약과 신규 안내 배너("이번 주 새로 추가된 미매칭 사업 2건이 있습니다")가 정확히 표시되는지 확인. B를 체크 해제하면 ②④⑤⑥·100% 체크박스 라벨(전체 금액까지 6.0억으로 감소 — 후보 목록 자체에서 제외됨)·미매칭 요약 바가 즉시 전부 재계산되는지 직접 확인, 재선택 시 원상 복구되는지 확인.
- 확도 체크박스(90% 해제) 시 ②④⑤⑥·메인 차트 범례("실적 예상")·보조 차트 범례가 동일하게 4.0억만큼 감소하고, ①연간 목표·③상반기 누적실적은 절대 변하지 않는지 확인(요청사항 7번 정확히 충족).
- 재분석 시나리오(주간 회의자료를 먼저 업로드한 뒤 인벤토리 분석)로 `UNMATCHED_100_SEEN`이 실제 미매칭 코드(B, C)만 정확히 담는지 확인 — 순서를 반대로(인벤토리 먼저) 했을 때는 매칭 전이라 A까지 일시적으로 미매칭으로 잡히는 것을 발견, 이는 실제 사용 흐름(인벤토리+매출자료를 함께 올리고 분석 실행)에서는 발생하지 않는 테스트 호출 순서 아티팩트임을 확인. 동일 데이터로 재분석 시 `UNMATCHED_100_NEW_CODES`가 빈 배열로 정확히 바뀌는지(이미 SEEN에 있으므로) 확인. `unmatched_100_excluded`/`unmatched_100_seen` idb 키가 페이지 새로고침 후에도 정확히 복원되는지 확인.
- 월별 팝업(23차 작업 기능)이 새 데이터 구조에서도 정상 동작(9월 클릭 시 B의 500백만 정확히 표시)하는지 회귀 확인. `clearAllStoredData()` 실행 후 `UNMATCHED_100_EXCLUDED`/`SEEN`/`NEW_CODES`가 모두 초기화되고 관련 idb 키가 삭제되는지 확인. 전 과정에서 콘솔 에러 없음.
- 이 환경의 브라우저 패널은 실제 레이아웃/페인트를 수행하지 않아(캔버스 크기가 항상 0×0으로 관측됨) 카드 스타일의 픽셀 단위 검증은 `className` 부여 여부와 CSS 규칙 존재 확인으로 대체.

---

### 2026-08-11 — 매출 예상 입력 적용 버튼 방식 변경 및 확인 필요 항목 우선 노출 (27차 작업)

#### 목표
매출 추이 탭 하단(주석·보조 차트·매출 예상 입력) 영역 개편. 간격이 답답했던 문제를 CSS로 해소하고, 지금까지 입력 즉시 반영되던 "매출 예상 입력" 표를 [적용] 버튼을 눌러야 실제 계산에 반영되는 명시적 방식으로 전환(중요 데이터 실수 방지). 매출예상월 미지정/예상금액 0/매출실적 미매칭 건을 "확인 필요" 영역으로 상단에 모아 우선 노출.

#### 1~2. 간격 조정 및 용어 정리
- `#rev2-content`를 `display:flex; flex-direction:column; gap:24px`로 변경 — 기존엔 `.chart-card` 사이에 별도 여백 규칙이 전혀 없어 카드가 맞닿아 있었음(스크린샷 없이 코드 감사로 발견). 이 변경만으로 "카드 사이 16px 이상"과 "주석-보조차트 24px 이상" 요구사항이 동시에 충족됨. `.rev2-summary-row`/`.rev2-validation-line`의 기존 개별 margin은 flex-gap과 중복되므로 0으로 정리.
- `.rev2-footnote`에 `line-height:1.9`, `margin-top`을 10px→13px로 소폭 확대. `#rev2-pipe-card`에 `padding-bottom:18px` 오버라이드 추가(공용 `.chart-card`는 16px라 전역 변경 대신 이 카드만 스코프).
- 보조 차트 제목·범례 구간명은 26차 작업에서 이미 "수주확도별 매출예상 구성"으로 변경 완료. 이번엔 그때 놓쳤던 **막대 자체의 데이터셋 라벨**("파이프라인 XX억")을 "실적 예상 XX억"으로 추가 수정(`renderPipelineCompositionChart`의 `labels:` 배열).

#### 3. 매출 예상 입력 — 적용 버튼 방식
- **임시본(draft) 구조 도입**: `REVENUE_FORECAST_DRAFT`(화면 입력칸이 직접 수정하는 사본) + `FORECAST_DIRTY_CODES`(사용자가 draft에서 실제로 건드린 사업코드 Set) 신규. 화면의 모든 입력(`updateForecastEntry`/`addForecastEntry`/`removeForecastEntry`)은 이제 `REVENUE_FORECAST_DRAFT`만 수정하고 `renderForecastSection()`만 다시 그림 — 요약카드·메인차트·보조차트·체크박스 라벨·월별 팝업이 참조하는 `REVENUE_FORECAST`(live)는 전혀 건드리지 않아 적용 전까지 다른 화면에 영향이 없음.
- **dirty 판정을 diff가 아닌 명시적 Set으로 설계**: 처음엔 "draft와 live를 통째로 비교"하는 방식을 고려했으나, 인벤토리 파일을 새로 올려 자동반영(`mergeAutoForecastFromInventory`)이 live를 변경하면 사용자가 손대지 않은 항목까지 "다르다"고 오판하는 문제가 있어 폐기. 대신 `FORECAST_DIRTY_CODES`를 사용자 입력 시점에만 명시적으로 추가하고, `syncForecastDraftFromLive()`(매 렌더링마다 호출)가 dirty가 아닌 코드만 live→draft로 동기화 — 외부 변경과 사용자 편집이 서로 덮어쓰지 않도록 분리.
- **[적용]**(`applyForecastChanges`): dirty 코드만 골라 `REVENUE_FORECAST_DRAFT`→`REVENUE_FORECAST`로 복사(둘 다 비어있는 항목은 삭제), 저장 후 `FORECAST_DIRTY_CODES.clear()`, `renderSalesTrend()` 전체 재실행. 적용 직전/직후의 하반기 예상실적(`h2TotalMillion`)과 수정된 사업코드별 변경 전/후 값을 `console.log`. 미적용 시 비활성화, dirty가 있으면 활성화 + `.rev2-forecast-apply-highlight`(accent 글로우) 부여.
- **[취소]**(`cancelForecastChanges`): dirty 코드만 draft를 live 값으로 되돌리고 Set을 비움.
- **[초기화]**(`resetForecastToAuto`): confirm 후 `REVENUE_FORECAST = {}` + `mergeAutoForecastFromInventory()` 재실행(파일의 매출 목록 시트에서 자동값을 처음부터 다시 구성) — 이 함수가 내부적으로 저장+`renderSalesTrend()`를 호출하므로 draft도 자동 동기화됨.
- **미적용 상태 안내**: 요약 바 아래 `#rev2-forecast-dirty-banner`(주황) "수정된 항목 N건 — [적용]을 누르지 않으면 반영되지 않습니다". 수정된 입력칸은 `.rev2-dirty-cell`(주황 테두리) — `isForecastEntryDirty(code, idx)`가 코드가 dirty일 때만 항목 단위로 live/draft를 비교해 정밀하게 판정.
- **이탈 방지**: `switchTab(t)`에 가드 추가 — 현재 탭이 'revenue-trend'이고 이동하려는 탭이 다르며 dirty가 있으면 confirm, 취소 시 탭 전환 자체를 막고(`return`) 그대로 머무름, 확인 시 `cancelForecastChanges()`로 draft를 버린 뒤 이동. `window.beforeunload`에 dirty일 때 `preventDefault()`하는 리스너 추가(브라우저 기본 이탈 경고).

#### 4. 확인 필요 항목 우선 노출
- `candidateReviewReasons(c, unmatchedCodeSet)` — draft 기준으로 판정(적용 전이라도 값을 채우면 즉시 목록에서 빠지도록): entries 중 `월` 없는 게 있으면 "월 미지정", `월`은 있는데 `금액`이 빈값/0이면 "금액 없음", 해당 사업코드가 26차 작업의 미매칭-100 목록에 있으면 "미매칭" — 완전 미입력(entries.length===0)은 별도의 "미입력" 집계로 남기고 확인 필요로 분류하지 않음(일반적/예상된 상태이므로).
- `renderForecastTableRows`가 필터링된 후보를 확인 필요/일반 두 그룹으로 나눠 각각 독립된 `<table>`(헤더는 동일 구조 중복 배치)에 렌더링. 확인 필요 영역(`#rev2-forecast-review-section`, 주황 배경)은 0건이면 완전히 숨김, 각 행에 사유 배지(`.rev2-review-badge`, 다건이면 여러 개) 표시.
- "전체 사업 N건 [더보기 ▾]" 버튼(`#rev2-forecast-more-btn`) — 기본 접힘(`FORECAST_NORMAL_EXPANDED`, idb 저장 없이 메모리 변수라 새로고침/탭 재진입마다 항상 접힘으로 시작). 검색어·구분필터·확도필터 중 하나라도 활성화되어 있으면 `FORECAST_NORMAL_EXPANDED`와 무관하게 자동 펼침, 검색어를 지우면(수동으로 편 적 없다면) 다시 접힘.
- 요약 바를 "전체 N건 · 자동반영 N건 · 수기입력 N건 · 확인필요 N건"으로 변경(기존 "미입력 N건" 세그먼트 제거, 확인필요는 `var(--del)` 주황).

#### 5. 연동 검증
- `computeForecastSummary`(26차 작업에서 이미 `h2TotalMillion`/`sectionSumH2`/`monthlySumH2`/`sectionMatch`/`monthlyMatch` 계산 및 콘솔 로그 보유)를 그대로 재사용 — `applyForecastChanges()` 이후 `renderSalesTrend()`가 이를 다시 호출하므로 "확도별 합계 = 하반기 예상실적 일치 여부"/"월별 합계 = 하반기 예상실적 일치 여부" 로그가 적용 직후 자동으로 다시 찍힘(중복 구현 없음). 화면 하단 `#rev2-validation-line` 문구("검증: 확도 합계 = 하반기 예상 ✓ / 월별 합계 = 하반기 예상 ✓")도 26차 작업 그대로 재사용.
- **데이터 참조 통일**: 요약 카드(`computeSummaryCardsV2`)·메인 차트(`renderSalesChart1`)·보조 차트(`renderPipelineCompositionChart`)·체크박스 라벨(`renderProbFilterUI`)·월별 팝업(`buildMonthForecastList`) 모두 `renderSalesTrend()` 한 번의 호출에서 계산된 동일한 `forecastData`(`computeForecastSummary(SALES_SCOPE)`) 객체를 인자로 전달받아 사용하는 기존 구조를 그대로 유지 — 이번 작업에서 별도의 원본 재파싱 경로를 추가하지 않았으므로 기준 불일치 위험이 구조적으로 없음(신규 코드 없이 기존 아키텍처 확인으로 충족).

#### 6. 저장
- 수기 입력값 저장 방식(사업코드 키, idb `revenue_forecast`)은 기존 그대로 — 이번 변경은 "언제 저장되는가"(입력 즉시 → 적용 시점)만 바뀌었을 뿐 저장 위치·키·구조는 무변경.

#### 검증
- 100% 구간 3건(매칭 1 + 미매칭 2) 등 6개 후보 픽스처로 검증. 페이지 진입 시 "확인 필요 2건"(미매칭 B·C만, 완전 미입력인 A/D/E/F는 제외됨)과 "전체 사업 4건"(기본 접힘)이 정확히 분리되는지 확인.
- B의 월/금액을 입력하면(적용 전) 사유 배지가 "월 미지정"+"금액 없음"에서 "미매칭"만 남고, 카드·체크박스 라벨·검증 라인은 전혀 변하지 않는지(적용 전 격리) 직접 확인 — [취소] 시 draft가 정확히 원상 복구(빈 값)되고 dirty가 0으로, [적용] 시 카드 6개(②42.0억/④12.0억/⑤-778.7억/⑥5.1%)·100% 체크박스 라벨(8.0억)·검증 라인이 모두 동시에 갱신되는지 확인. 콘솔에 "[매출 예상 적용] 수정된 사업"과 "하반기 예상실적 — 변경 전: 0 백만 → 변경 후: 1200 백만" 로그가 정확히 출력되는지 확인.
- 이탈 방지: `switchTab('revenue-trend')`로 진입한 상태에서 dirty를 만든 뒤 다른 탭으로 이동 시도 — confirm 취소 시 탭에 그대로 머물고 dirty 유지, confirm 수락 시 이동하며 draft가 취소(원복)되는지 확인. `beforeunload` 이벤트를 직접 dispatch해 dirty일 때 `preventDefault()`가 호출되는지 확인.
- [초기화] confirm 수락 시 `REVENUE_FORECAST`/`REVENUE_FORECAST_DRAFT`/dirty가 모두 파일 기준 상태로 정리되는지 확인.
- 검색어 입력 시 "전체 사업" 영역이 자동으로 펼쳐지고 결과가 필터링되며, 검색어를 지우면(수동으로 편 적 없으므로) 다시 접히는지, 수동으로 [더보기]를 누르면 펼쳐지는지 확인. 페이지 새로고침 후 `FORECAST_NORMAL_EXPANDED`가 항상 `false`로 시작하고, `REVENUE_FORECAST_DRAFT`가 복원된 `REVENUE_FORECAST`와 정확히 일치(dirty 없음)하는지 확인. 전 과정에서 콘솔 에러 없음.

---

### 2026-08-11 — 타이틀 및 탭 구조 개편, 매출 비교 탭 제거 (28차 작업)

#### 목표
서비스 브랜드/타이틀을 "파이프라인 주간 변동이력"에서 "OKC Business Intelligence"로 전면 개편하고, 탭 구조를 3개(인벤토리 주간변동 분석 / 26Y 실적예상 분석 / 기타 분석(개발 중))로 정리. 더 이상 쓰이지 않는 "매출 비교" 탭을 제거.

#### 1. 타이틀 변경
- `<h1>`: "파이프라인 주간 변동이력" → "OKC Business Intelligence", `<title>`(브라우저 탭)도 동일하게 변경.
- 부제: "전주 대비 금주 금액 변동 · 신규 등록 · 삭제 항목 분석" → "인벤토리 주간변동 및 26년 실적예상 분석".
- 좌측 로고 배지: "P" → "BI"(2글자에 맞춰 `font-size` 16px→13px, `letter-spacing` 축소).
- 영문 타이틀과 한글 부제의 균형을 위해 `h1`의 `font-size`를 20px→18px로 줄이고 `letter-spacing`을 -0.3px(한글 자간 좁힘)에서 +0.2px(영문 자간 살짝 벌림)로 전환.

#### 2. 탭 구조 변경
- 순서를 인벤토리 주간변동 분석 → 26Y 실적예상 분석 → 기타 분석(개발 중)으로 재배치(기존: 인벤토리 비교 → 매출 비교 → 변동보고서 → 매출 추이).
- `data-tab` id 자체는 유지하고(`inventory`/`revenue-trend`/`report`) 버튼 라벨 텍스트만 변경 — `switchTab()`/`VALID_TAB_IDS`/idb 저장 키 등 내부 로직을 건드리지 않기 위한 선택.
- "기타 분석 (개발 중)" 탭에 `.tab-muted` 클래스 추가, 비활성/활성 상태 모두 회색 계열(`#5a5f78`/`#7a80a0`)로 고정해 다른 두 탭과 시각적으로 구분.

#### 3. 매출 비교 탭 제거
- 탭 버튼과 `<div id="tab-sales">` 콘텐츠를 HTML에서 제거하되, 6차 작업(파이프라인 추이 탭 제거) 때와 동일한 방식으로 `<!-- -->` 주석으로 통째로 보존(복구 시 주석 해제만 하면 되도록 안내 주석 포함).
- 관련 JS 함수(`setupCard`/`processFile`/`checkReady`/`resetAll`/`detectConfig`/`runCompare`/`renderResult`/`applyFilter`/`applySearch`/`exportCsv`/`saveSalesCompareData`/`restoreSalesCompareData`/`clearSalesCompareStorage`)도 삭제하지 않고 `/* ... */` 블록 주석으로 보존. 단, 이 함수들 안에 있던 `/* 무시 */` 같은 인라인 블록 주석은 JS가 중첩 블록 주석(`/* /* */ */`)을 지원하지 않아 바깥쪽 주석을 조기 종료시키는 문제가 있어, 주석 처리 전에 전부 `//` 라인 주석으로 바꿔야 했음(직접 문법 오류로 발견).
- `toNum()`은 이름은 비슷한 위치에 있었지만 매출 추이 탭(`readAmountsFromRow`/`parseTeamDetailRows`)에서도 쓰이는 공용 함수라 주석 처리 대상에서 제외.
- `DOMContentLoaded`에서 `setupCard('sal','prev')`/`setupCard('sal','curr')` 호출 제거, `await restoreSalesCompareData()`를 신규 `purgeRemovedSalesCompareStorage()`(idb `sales_compare` 키를 조용히 삭제)로 교체. `clearAllStoredData()`에서 `resetAll('sal')` 호출 제거(헤더의 "저장 데이터 전체 삭제" 대상에서 자연히 제외됨).
- "공유용 HTML 내보내기" 기능은 코드 전체를 검토한 결과 현재 코드베이스에 애초에 존재하지 않아 해당 사항 없음(요청의 예방적 조항으로 판단, 별도 조치 불필요).

#### 4. 기타 분석 (개발 중) 탭
- 기존 `<div id="tab-report">`의 내부를 `.report-placeholder`(중앙 정렬, "준비 중입니다" + "추가 분석 기능이 개발 중입니다." 회색 텍스트)로 교체하고, 원래 콘텐츠(보고서 생성 버튼 + report-content)는 같은 `id="tab-report"` 아래 `<!-- -->` 주석으로 바로 아래에 보존.
- `generateReport`/`buildReportSection`/`buildProjectCard`(변동보고서 전용, "─── 변동보고서 ───" 섹션 전체)를 `/* ... */`로 주석 처리, `switchTab()`의 `if (t === 'report') generateReport();` 호출도 제거(주석 처리된 함수를 호출하면 즉시 런타임 에러가 나므로).
- 이 섹션과 이름이 비슷해 혼동하기 쉬운 "보고문 초안"(`buildFullReportDraftText`/`copyReportDraft`, 인벤토리 비교 탭 안에 있는 완전히 다른 기능)은 그대로 유지 — 코드 감사로 두 기능이 서로 다른 섹션(주석 헤더 "인벤토리 비교: 보고문 초안" vs "변동보고서")에 있음을 확인 후 후자만 정확히 골라 주석 처리.

#### 5. 마지막 탭 기억 보정
- `VALID_TAB_IDS`에서 `'sales'` 제거(`['inventory','report','revenue-trend']`) — `restoreLastActiveTab()`이 저장된 값을 이 배열로 검증하는 기존 구조 덕분에, 저장값이 `'sales'`였던 사용자는 별도 코드 추가 없이 자동으로 첫 탭('inventory')으로 대체됨.
- `switchTab(t)` 최상단에 `if (!document.getElementById('tab-' + t)) t = 'inventory';` 방어 코드 추가 — `restoreLastActiveTab()` 경로 외에 어디선가 유효하지 않은 탭 id로 직접 호출되더라도(예: 브라우저 콘솔, 향후 코드 변경 실수) 에러 없이 안전하게 첫 탭으로 대체.

#### 검증
- 헤더 로고("BI")·타이틀("OKC Business Intelligence")·부제·탭 3개(라벨/순서/`tab-muted` 클래스)를 DOM에서 직접 조회해 스펙과 정확히 일치하는지 확인.
- 제거 대상 함수 16개(`setupCard`~`buildProjectCard`) 전부를 `typeof window[name]`로 조회해 `'undefined'`임을 확인 — 코드가 주석 안에 안전하게 격리되어 있고 실수로 활성 코드에 남지 않았음을 런타임으로 검증.
- "기타 분석" 탭 전환 시 "준비 중입니다"/"추가 분석 기능이 개발 중입니다." 텍스트와 `#report-content` 요소가 더 이상 존재하지 않는지 확인.
- `idbKeyval.set('last_active_tab','sales')` 후 `restoreLastActiveTab()` 실행 → 자동으로 'inventory' 탭이 열리는지 확인. `switchTab('sales')`를 직접 호출해도 에러 없이 'inventory'로 대체되는지 확인.
- `sales_compare` idb 키에 더미 데이터를 심어둔 뒤 새로고침 → 페이지 로드 시 조용히 삭제되는지 확인.
- 인벤토리 탭에서 실제 분석을 실행한 뒤 `clearAllStoredData()`를 호출했을 때 `TypeError: Cannot set properties of null (setting 'innerHTML')`(report-content 참조 오류)가 발생하는 회귀를 최초 테스트에서 실제로 발견 — `resetInvAll()`이 이미 제거된 `#report-content`를 초기화하려던 잔재 코드였음을 확인하고 해당 줄을 삭제해 수정. 재검증 결과 에러 없이 정상 동작.
- 인벤토리 비교(파일 업로드 → 분석)와 26Y 실적예상 분석(탭 전환 → 렌더링) 두 탭이 이번 변경 이후에도 회귀 없이 정상 동작하는지 확인. 전 과정에서 콘솔 에러 없음.

---

### 2026-08-12 — 월별 팝업 회차 표기 및 개별 제외 기능 추가 (29차 작업)

#### 목표
매출 추이 탭의 월별 팝업(23차 작업)에 두 기능 추가: ①각 항목이 해당 사업의 몇 번째 매출 회차인지 "(N/M)" 형태로 표기 ②사업+월 단위로 개별 항목을 매출예상 집계에서 즉시 제외/복원할 수 있는 체크박스. 매출 예상 입력 표에서도 동일한 제외 상태를 공유해 표시·해제할 수 있게 함.

#### 1. 회차 표기
- `parseSalesListSheet`의 매출순번 파싱에 `normalizeSeq(v)` 신규 헬퍼를 적용 — 셀 값이 실수(1.0/2.0)로 저장돼 있어도 `Number()` 변환 후 `Math.trunc`로 "1"/"2" 형태의 안정적인 문자열 키로 정규화(값이 숫자가 아니면 원본 문자열 그대로 폴백). 매출순번 키가 매주 흔들리면 `mergeForecastForCode`의 `__seq` 매칭이 끊겨 같은 청구 건이 매주 새 항목으로 오인될 수 있어 파싱 시점에 정규화가 필요했음.
- `mergeForecastForCode(code, freshEntries)`에서 `seqTotal = freshEntries.length`(이번 주 매출 목록 기준 해당 코드의 전체 매출 행 수 — 연도 무관, 2027년 이후 건도 포함)를 계산해 병합되는 모든 항목(자동 갱신분·수기 보존분 모두)에 `__seqTotal` 필드로 저장. 수기로 수정한 항목(`auto:false`)도 월/금액은 그대로 보존하되 `__seqTotal`만 최신값으로 갱신(스프레드 연산자로 병합).
- 월별 팝업(`buildMonthForecastList`)이 각 항목에서 `__seq`/`__seqTotal`을 꺼내 "(1/2)" 형태로 표기. CSS를 `.rev2-month-popup-name`을 flex 컨테이너로 재구성해 사업명(`.nm`)만 `text-overflow:ellipsis`로 말줄임하고, 회차(`.rt`)·담당자(`.rep`)는 `flex-shrink:0`로 절대 잘리지 않도록 분리.

#### 2~4. 개별 제외 체크박스(사업+월 단위, 즉시 반영)
- `EXCLUDED_REVENUE_ITEMS`(Set, 키 `"사업코드_YYYY-MM"`) 신규 전역 상태. `isRevenueEntryExcluded(code, month)`/`toggleRevenueItemExcluded(code, month, included)` — 토글 즉시 idb 저장(`excluded_revenue_items` 키) + `renderSalesTrend()` 전체 재계산 + 팝업이 열려 있으면 본문만 재렌더링(`refreshMonthForecastPopup`). [적용]/[취소] 버튼 없이 항상 즉시 반영(27차 작업의 매출 예상 입력 draft/apply 구조와는 의도적으로 분리된 별도 메커니즘).
- `computeForecastSummary`의 두 항목 필터 루프(체크박스 라벨용 `sectionAmounts2026` 집계, 실제 카드/차트용 `bySectionMillion`/`h2TotalMillion` 집계)에 `!isRevenueEntryExcluded(code, e.월)` 조건을 추가 — 이 한 곳의 수정으로 카드 6개·메인 차트 매출예상선·보조 차트·확도 체크박스 라벨·월별 팝업 전부에 제외가 자동으로 전파됨(모두 이 함수 하나를 공유하는 기존 아키텍처를 그대로 활용).
- 월별 팝업(`buildMonthForecastList`)은 제외된 항목도 걸러내지 않고 `included` 플래그만 붙여 그대로 반환 — 팝업은 "보여주고 복원 가능하게" 해야 하므로 집계 함수의 필터링 목적과 다르게 설계. 팝업 행에 체크박스(맨 왼쪽, 확도 배지 앞) 추가, 제외 항목은 회색(`opacity:0.45`)+취소선. 헤더를 "N건 · X.X억"에서 "N건 중 M건 포함 · X.X억"으로 변경. 하단에 회색 안내 "체크 변경은 즉시 반영됩니다" 추가. 팝업 닫기 시 확인창 없음(기존과 동일, 추가 변경 없음).
- 매출 예상 입력 표(`buildForecastRowsHtml`)에도 "포함" 체크박스 컬럼을 신규 추가(월이 지정된 항목에만 표시) — 팝업과 동일한 `EXCLUDED_REVENUE_ITEMS`를 그대로 참조해 두 화면이 완전히 같은 데이터를 공유(별도 상태 없음, 원본 시트 재파싱 없음). 제외된 행은 `rev2-forecast-row-excluded` 클래스로 월/금액 셀이 흐리게+취소선 처리.
- 저장: idb 키 `excluded_revenue_items`에 배열로 저장, `DOMContentLoaded`에서 `restoreExcludedRevenueItems()`를 `restoreInvFiles()`보다 먼저 호출(다른 즉시-복원 상태들과 동일한 순서 원칙). 인벤토리에서 사업이 사라져도 Set에서 자동 정리하지 않음(다시 나타날 수 있으므로) — `clearAllStoredData()`를 눌러야만 삭제.

#### 5. 제외 현황 안내
- 메인 차트 아래에 신규 각주 `#rev2-manual-excluded-footnote` 추가(기존 "미매칭 100%" 각주 패턴을 그대로 재사용) — "※ 수동으로 제외한 매출예상 N건(XX억)이 있습니다. [상세보기]", 0건이면 숨김. `[상세보기]` 클릭 시 사업코드/사업명/월/금액/[복원] 버튼으로 구성된 표가 펼쳐짐(`buildManualExcludedList(scope)`가 현재 스코프의 후보 목록을 기준으로 제외 항목만 수집). 기존 "2027년 이후 자동 제외" 각주(`rev2-excluded-footnote`)와는 별개의 기능이라 별도 id로 분리.

#### 검증
- 로컬 정적 서버 + 합성 픽스처(OP-SEQ-001: 매출순번 1.0/2.0으로 2건 분할, 2026-09/2027-02 — 스펙의 OP-26-0338 예시와 동일한 연도교차 케이스 / OP-SEQ-002: 2026-10/2026-12로 같은 해 다른 달 2건 분할, "사업+월" 단위 독립 제외 검증용)로 브라우저에서 실제 업로드→분석까지 실행.
- `normalizeSeq`가 실수(1.0/2.0)를 "1"/"2"로 정규화하고, `__seqTotal`이 연도 무관 전체 건수(2건)로 정확히 계산되는지 `REVENUE_FORECAST` 직접 조회로 확인. 9월 팝업에서 "(1/2)", 12월 팝업에서 "(2/2)" 표기가 스펙 예시와 정확히 일치, 회차·담당자가 사업명 말줄임과 무관하게 잘리지 않는 것을 화면에서 직접 확인.
- 12월 항목만 체크 해제 → 10월(같은 사업 OP-SEQ-002의 다른 회차)은 영향받지 않고 그대로 포함 유지되는지 확인(사업+월 단위 독립성 검증), `h2TotalMillion`이 정확히 300백만 감소하는지, 열려 있던 팝업이 즉시 "1건 중 0건 포함 · 0.0억"으로 갱신되는지, idb에 `["OP-SEQ-002_2026-12"]`가 저장되는지 확인.
- 사업의 모든 회차를 제외하면 해당 사업이 카드/차트 집계에서 "미입력" 취급으로 자연스럽게 전환되는지(수주금액 전액이 `noInputAmountMillion`으로 이동) 확인. 제외 상태에서 페이지를 완전히 새로고침한 뒤 파일을 다시 업로드(다음 주 시나리오 재현)해도 같은 사업+월 조합의 제외 상태가 그대로 유지되는지 확인.
- 각주 "수동으로 제외한 매출예상 1건(3.0억)이 있습니다. [상세보기]"가 정확히 표시되고, 상세 목록에서 [복원] 클릭 시 즉시 포함 상태로 되돌아가는지 확인. 매출 예상 입력 표의 "포함" 체크박스를 실제 DOM 클릭(좌표 클릭 + `.click()` 양쪽 모두)으로 조작해 팝업과 동일하게 즉시 반영되는지 확인.
- `clearAllStoredData()` 테스트 중 `EXCLUDED_REVENUE_ITEMS_KEY`에 대한 `idbKeyval.del()` 호출이 누락되어 메모리 상태는 초기화되지만 idb에는 이전 값이 그대로 남는 버그를 발견 → `clearAllStoredData()`에 `del` 호출을 추가해 수정, 재검증으로 idb까지 정확히 삭제됨을 확인.
- 전 과정에서 콘솔 에러 없음. `node -e "new Function(...)"` 구문 검사 통과.

---

### 2026-08-12 — 제외된 매출예상 항목 표시 방식 정의 (30차 작업)

#### 목표
29차 작업에서 추가한 매출 예상 입력 표의 "포함" 체크박스가 값은 유지하되 배경 어둡게 처리는 빠져 있었음 — 표시 방식을 명시적으로 정의하고 누락된 부분을 보완.

#### 정의된 표시 방식
- 체크박스 해제 시 예상금액·매출예상월 값은 절대 지우지 않고 그대로 유지(이미 29차 작업부터 구조적으로 보장 — `toggleRevenueItemExcluded`는 `EXCLUDED_REVENUE_ITEMS` Set만 변경하고 `REVENUE_FORECAST` 항목 자체는 건드리지 않음). 값이 남아있어야 나중에 복원 여부를 판단할 근거가 되므로 값을 비우면 안 됨.
- 체크박스: 해제 상태로 표시.
- 예상금액 입력칸: 값 유지 + 회색(투명도 낮춤) + 취소선.
- 매출예상월 드롭다운: 동일하게 회색 처리(+취소선).
- **행 배경을 살짝 어둡게 처리(신규 추가)** — `.rev2-forecast-table tr.rev2-forecast-row-excluded td { background: var(--bg); }`를 CSS에 추가. 기존 자동반영 셀 배경(`.rev2-auto-cell`)이나 분할청구 서브행 배경(`.rev2-forecast-subrow`)보다 CSS 특이도가 높도록 선택자를 구성(`tr.클래스 td` 2요소 vs 기존 규칙들의 1요소)해 별도 순서 조정이나 `!important` 없이 항상 우선 적용되도록 함.
- 체크 재활성화 시 `EXCLUDED_REVENUE_ITEMS`에서 키만 제거 — `REVENUE_FORECAST`의 월/금액 값은 애초에 변경된 적이 없으므로 재입력 없이 즉시 원상태로 복귀(기존 구조가 이미 만족).
- 제외된 행은 `computeForecastSummary`의 하반기(7~12월) 집계 루프에서 계속 필터링되므로 "하반기 예상실적" 카드에 반영되지 않음(29차 작업에서 이미 구현, 이번엔 재확인만).

#### 검증
- 합성 픽스처(OP-SEQ-002, 2026-12 항목 300백만)로 실제 체크박스 해제 → `.rev2-amount-input.value`가 여전히 `"300"`, `.rev2-month-select.value`가 여전히 `"2026-12"`로 남아있는지, 셀 배경색이 `getComputedStyle`로 `rgb(15, 17, 23)`(`var(--bg)`)로 정확히 바뀌는지 직접 확인.
- 하반기 예상실적 카드가 11.0억 → 8.0억(300백만 제외)으로 정확히 줄어드는지 확인. 체크박스를 실제 DOM 클릭으로 다시 켜서 재입력 없이 11.0억으로 즉시 복원되는지, 값이 그대로였는지 확인. 스크린샷으로 어두워진 행 배경·취소선·해제된 체크박스를 시각적으로 확인.
- 전 과정에서 콘솔 에러 없음. `node -e "new Function(...)"` 구문 검사 통과.

---

## 환경 정보
- OS: Windows 10
- Node.js: v24.16.0
- GitHub CLI: v2.93.0
- Git: v2.54.0
- Claude Code: v2.1.169
- 계정 이메일: soyea0529@gmail.com
